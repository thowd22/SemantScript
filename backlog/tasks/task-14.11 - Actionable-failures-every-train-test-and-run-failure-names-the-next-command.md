---
id: TASK-14.11
title: 'Actionable failures: every train, test and run failure names the next command'
status: Done
assignee:
  - '@claude'
created_date: '2026-09-25 15:21'
updated_date: '2026-09-26 05:19'
labels:
  - dx
  - debug
milestone: m-5
dependencies: []
parent_task_id: TASK-14
ordinal: 64000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The diagnostics catalogue lists every error with a cause and a fix, but the tools themselves do not say it: a verification failure prints the failing cases and stops, a runtime SemaRuntimeNotLoadedError says only that no artifact is loaded, and the trainer's Python tracebacks reach the terminal unwrapped when the interpreter or a package is missing. A developer should not need the catalogue to know what to do next.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Every verification failure line in the train report ends with a specific suggestion derived from the evidence: the constraint to add for a rule the misses share, an example for a repeated miss, more cases or epochs for a calibration failure, or the seed retry
- [x] #2 Runtime errors for a missing or stale artifact name the command that fixes them, and the CLI wraps trainer and interpreter failures into one line with the doctor check to run
- [x] #3 The diagnostics catalogue's fix column and the tools' messages are generated from one source so they cannot drift
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. One source (AC3). New diagnostics/remedies.json: every remedy the tools print, as entries {id, family, match (the catalogue's failure/class column), cause, fix}. fix is a template with {name} placeholders the tools fill from evidence; the docs render a placeholder as <name>. Families: verifier (gold-rule-constraint, gold-repeated-miss-example, gold-check-example, violation-example, violation-denser-data, calibration-cases-epochs, seed-retry-attempts, seed-retry-next-seed, type-no-cache), trainer-process (python-missing, module-missing per doctor check id trainer/model/torch/onnxruntime, python-too-old, trainer-crash), runtime (not-loaded, artifact-missing, one per ArtifactLoadError code, unknown-function/stale), cli (test-no-artifact, test-example-mismatch, test-function-absent, run-no-export, build-no-tsconfig, build-no-outdir). New scripts/generate-remedies.mjs writes runtime/src/remedies.generated.ts, trainer/src/semantscript_trainer/remedies_generated.py and the generated tables in docs/diagnostics.md between <!-- remedies:<family>:begin/end --> markers (prettier-padded), and with --check exits 1 naming each stale file. Drift tests: runtime/test/remedies.test.mjs runs --check (node CI job) and trainer/tests/test_remedies.py compares the generated Python module with the JSON (python CI jobs). Small formatters: remedy(id, params) in runtime/src/remedies.ts (exported from @semantscript/core for the CLI) and semantscript_trainer/remedies.py; both reject unknown ids and missing or extra params. Doctor check fixes and TS91xx compiler fixes stay hand-written (their tools do not print a catalogue fix); stated in the catalogue.
2. Verification suggestions (AC1), trainer/src/semantscript_trainer/verification.py + new remedies.py derivation helpers (pure, torch-free): VerificationResult gains suggestions: tuple[str, ...] (compare=False, not serialized to IR/manifest; empty or one per failure, validated). evaluate_training_result gathers structured evidence (gold misses: inputs/expected/predicted; corpus rows mispredicted the same way; constraint violations per constraint index with required output) and derives: (a) gold misses (>=2) sharing expected output and a single-field predicate (string/bool equality, numeric >= / <= threshold) that holds for every miss and every corpus row it covers is labelled that output, not already a constraint -> 'add always(() => order.status === "cancelled", "deny")'; (b) otherwise the most frequent teacher-labelled row mispredicted like a gold miss -> 'add examples entry { inputs: {...}, output: ... }'; else check the example against the teacher (explain is TASK-14.7 follow-up); (c) a constraint violated by >=2 records -> example literal with the output an always() demands (never(): the teacher label); single violations -> denser boundary data (--cases 2n, --counterfactual-ratio); (d) ECE -> --cases <2n> or --epochs <e+k> with current values; (e) type errors -> --no-cache.
3. Seed retry suggestion, trainer cli.py: after seed_retry_decision, a narrow failure that stopped because the retry was off or exhausted replaces the ECE/violation suggestion with seed-retry-attempts (--seed-attempts <k>) or seed-retry-next-seed (--seed <n+1>). _function_report/_attempt_report add verification.suggestions and each failure string in the report ends with '\n  next: <suggestion>' (so the JSON and the rendered report both end with it); the error: verification failed stderr line names the first suggestion. Test in trainer/tests/test_remedies.py (derivation units with synthetic rows) and test_seed_retry.py/test_cli.py (report shape) without torch where possible; a torch test for evaluate_training_result suggestions skips cleanly without the extra.
4. CLI render + trainer wrapping (AC2), cli/src/train.ts: renderTrainReport prints the suggestions (next: line after each failure block; tolerate reports without the field). runProcess pipes stderr: forwards lines live, buffers from 'Traceback (most recent call last):', and on a nonzero exit classifies the last exception line (ModuleNotFoundError name -> doctor check id trainer/model/torch/onnxruntime; SyntaxError/version -> python; other -> trainer-crash) into one line: 'semantscript train: the trainer stopped: <Exc: msg>; run semantscript doctor (check <id>) ...; full traceback in <report>.traceback.txt'. spawn ENOENT -> python-missing line with the doctor check. Same wrapping for --estimate's captured stderr. New fixture behaviour in cli/test/fixtures/fake_trainer.py (FAKE_TRAINER_RAISE=module:torch|crash) and cli tests.
5. Runtime (AC2): SemaRuntimeNotLoadedError message names loadSemaArtifact() and semantscript run and carries remedy; ArtifactLoadError gets readonly remedy (per code; missing artifact directory/ENOENT -> semantscript train; INCOMPATIBLE -> semantscript train; INVALID_POINTER/INTEGRITY/INVALID_JSON/INVALID_MANIFEST -> semantscript releases rollback <release> or train; RESOURCE -> semantscript doctor --runtime; QUOTA -> maximum*Bytes option) appended to message, plus readonly detail (message without the remedy) that cli/src/releases.ts RELEASE_REJECTED uses so a refusal does not advise rollback inside rollback; SemaUnknownFunctionError (stale: program rebuilt after training) names semantscript build then semantscript train. Existing regex assertions keep matching (messages only gain a suffix). Tests in runtime/test/artifact-loader.test.mjs and inference-runtime/runtime tests.
6. CLI test/run/build: test with no current.json -> test-no-artifact line (semantscript train, or --artifact); a replayed example mismatch and an absent function -> semantscript build && semantscript train; run with a missing export lists the exports; build: missing tsconfig -> semantscript init, missing outDir -> fix. All texts via remedy(). cli.test.mjs cases for each.
7. Docs: docs/diagnostics.md (generated fix columns for verifier gates, new trainer-process and CLI failure tables, runtime table; how the next: line is derived; note that remedies.json is the source and how to regenerate), cli-reference.md (train next: lines, traceback file, test/run messages), environment.md (wrapped trainer failure line), docs/CONTRIBUTING.md (npm/node scripts/generate-remedies.mjs, --check in tests), runtime/README.md (remedy/detail), trainer/README.md (verification.suggestions, failure strings), cli/README.md if it lists output. npx prettier@3.9.9 --check docs README.md.
8. Verify: npm run build, npm run lint:node, npm run test:node, python ruff check/format --check, pytest trainer/tests, node scripts/generate-remedies.mjs --check; one local end-to-end probe with --teacher constraints on a tiny bundle forcing a failing gate (e.g. --ece-threshold 0.0001, --seed-attempts 1) to show real next: lines (no paid teacher). Commit on task-14.11, push, gh run watch.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Slice 1 done: diagnostics/remedies.json + scripts/generate-remedies.mjs (writes runtime/src/remedies.generated.ts, trainer remedies_generated.py, docs/diagnostics.md tables; --check for drift). Runtime remedy()/remedyFamily() exported; ArtifactLoadError gains remedy/detail, SemaRuntimeNotLoadedError and SemaUnknownFunctionError gain remedy + '; next:' clause; releases RELEASE_REJECTED uses detail. Trainer: suggestions.py derivation (shared one-field rule -> always(), repeated miss -> examples entry, lone miss -> check example, violations -> example or denser data, ECE -> --cases/--epochs, narrow stop -> --seed-attempts/--seed), VerificationResult.suggestions, report failures end with '\n  next: ...'. runtime tests 110/110 incl. remedies.test.mjs; pytest test_remedies/test_seed_retry/test_cli/test_verification pass.

Slice 2 done: CLI train pipes trainer stderr (TrainerStderr forwards lines live, holds a traceback or 'No module named' launch line), classifies it (module -> doctor check trainer/model/torch/onnxruntime, SyntaxError -> python, else trainer-crash) and prints one line 'semantscript train: the trainer stopped: <exc>; next: <remedy>; full traceback in <report>.traceback.txt'; spawn ENOENT gets the python-missing remedy; --estimate wraps the same way. test: no current.json -> test-no-artifact, absent function / example mismatch -> next: lines (and JSON next[]); run: missing export lists exports; build: missing tsconfig -> init, missing outDir -> config. Docs: diagnostics.md generated tables (trainer-process, verifier, cli, runtime) + derivation text, cli-reference, environment, CONTRIBUTING (Generated remedies), docs/index, runtime and trainer READMEs. Local probe (constraints teacher, --ece-threshold 0.0001 --seed-attempts 1 --epochs 2, no paid calls) printed real next: lines under all three failures; real interpreter launch failure (--trainer-module semantscript_missing.cli) printed the one-line wrap. Checks: npm run build, lint:node, test:node (89/110/33/8/85 pass), ruff check+format, pytest 662 passed 4 skipped, generate-remedies --check, prettier docs.

Committed bc0d4b5 on task-14.11 and pushed; CI run 36214220992 green (Node, Python dev and dev+training, doctor windows/macos, fresh install). Acceptance criteria not yet checked (left for the finalization stage).

Fix round 1 (review findings): (1) handled trainer failures: new semantscript_trainer/failures.py adds '; next: <fix>' to the trainer's own error: line for a missing training package (walks the exception chain; ModuleNotFoundError name or 'training extra' guard -> doctor check torch/onnxruntime/model, remedy training-extra-missing), a missing or unreadable bundle (bundle-unreadable), a malformed bundle (new InvalidBundleError -> bundle-invalid), a broken teacher file (teacher-config-invalid) and a teacher transport error (teacher-transport); --estimate too. The CLI passes SEMANTSCRIPT_DOCTOR_COMMAND so the printed doctor command carries --python/--trainer-module. (2) Stale report: train stats the report before and after the run and renders only a report this run wrote. (3) TrainerStderr no longer treats every traceback as fatal: a held traceback is released in place when an ordinary line follows its exception line, and at exit only a traceback that ends the run without a fresh report becomes the one-line wrap; carriage-return progress lines are forwarded live; the trainer's own 'error: ... No module named' line is not a launch failure. SyntaxError blames the Python version only when the interpreter is older than 3.12. (4) AC3: new doctor (30 remedies) and editor (6) families in diagnostics/remedies.json; cli/src/doctor.ts, trainer doctor.py and compiler ts-plugin.ts (9150-9153) print their fixes through remedy(); the generator writes the full module (table + formatter) into runtime/src and compiler/src, and the doctor and editor tables in docs/diagnostics.md are generated; only the compiler TS91xx advice column stays hand-written (the compiler prints no fix). CLI remedy lookup now goes through @semantscript/compiler synchronously. (5) semantscript test: an unreadable pointer or release names releases rollback (test-artifact-unreadable), an unverified function adds a next: line. Docs: diagnostics.md, cli-reference.md, environment.md, CONTRIBUTING.md, cli/README.md. Tests: trainer/tests/test_failures.py, cli.test.mjs (stale report, noisy traceback exit 0 and 3, filter unit tests, doctor command, corrupt/dangling artifact in test), ts-plugin.test.mjs. Checks: build, lint:node, test:node (89/110/35/8/85), pytest 666 passed 4 skipped, ruff, generate-remedies --check, prettier docs. Real probe: shadowed torch via --python wrapper after a real failed training -> 'error: fine-tuning requires Transformers; install the project training extra; next: run semantscript doctor --python <wrapper> and fix its torch check: /usr/bin/python3 cannot import torch', exit 1, no stale report rendered.

Fix round 1 committed as 18e4a18 on task-14.11 and pushed; CI run 36215895010 green on all six jobs. Acceptance criteria still unchecked (finalization stage).

Fix round 2 (review findings, all 8 blocking verified before fixing): (1) a trainer killed by a signal (OOM SIGKILL, native SIGSEGV) now prints 'semantscript train: the trainer was killed by <signal>; next: <trainer-killed>' and exits 128+n; runProcess and capture keep the signal, and a traceback the trainer survived is no longer blamed. (2) every failure the trainer catches names a fix: failures.py classifies out of memory, unwritable paths (the OSError filename, collapsed to the cache dir), export failures, option range errors (new 'options' stage, configs built before the bundle is read, seed-retry flags too), rejected teacher answers, constant constraints, and a last-resort train-failed; the constraints-teacher, gold-example, single-group, few-distinct, spend-cap and price-unknown messages now end with '; next:' from remedies.json. (3)+(5) suggestions: no constraint suggestion for a non-scalar output (TS9122), and none when an active always() already requires that output on every missed and covered row (verification passes a required-outputs evaluator). (4) build-no-tsconfig now names 'npx -p typescript tsc --init --rootDir . --outDir dist, then semantscript init' (init does not create a tsconfig); malformed tsconfig gets build-tsconfig-invalid. (6) the printed doctor command carries --teacher, and python-missing uses it. (7) violation-denser-data no longer suggests a counterfactual ratio above 1 or cites decision-8. (8) docs/diagnostics.md: the hand-written bundle-driver, constraints-teacher and spend tables are replaced by the generated 'teacher' family table; the remaining hand-written columns (compiler Fix, seed-retry What to do, runtime Advice) are for messages that print no fix, and the intro states that scope. New cli remedies train-no-teacher and teacher-probe-none. Real probes (USD 0): signal modules, bad teacher file, missing python, /dev/null cache, option errors, the already-stated-rule refund program (now suggests an example), and following the tsconfig advice builds.

Fix round 2 committed as 126f0e5 on task-14.11 and pushed; CI run 36218187922 green on all six jobs. Acceptance criteria still unchecked (finalization stage).

Fix round 3: (1) a failed Hugging Face encoder/tokenizer download (offline, proxy, outage, bad model id) is no longer sent to train-path-unwritable: failures.py detects huggingface_hub errors and the hub's download text and prints the new encoder-download-failed remedy; network OSErrors (requests/urllib3/httpx, ConnectionError, TimeoutError) are no longer treated as unwritable paths. Probe: HF_HOME=<empty> HF_HUB_OFFLINE=1 train --teacher constraints --cases 20 --epochs 1 --no-preflight printed the tokenizer error and 'next: check that this machine can reach https://huggingface.co ...'. (2) artifact-corrupt, test-artifact-unreadable and editor-unreadable now say 'run semantscript releases list to find an intact release, then switch to it with semantscript releases rollback <release> ...'; test-function-unverified names rollback <release> too. Probe: corrupt current.json -> run printed the new line; releases list, then releases rollback d642b9dab4b1 --artifact art4 succeeded and run returned "high". (3) New underfit-more-training remedy: when the head misses more than 5% (at least 3) of its teacher-labelled training rows or predicts one output for all of them, the gold-miss and constraint gates name --epochs doubled (at least 3) and --cases doubled (at least 64) instead of one more example. Probe on the tier program: --cases 8 --epochs 1 printed 'rerun with --epochs 3 (now 1) and --cases 64 (now 8) ...: the head gets 7 of 19 teacher-labelled training cases wrong (it predicts "high" for every one)'; following it (--cases 64 --epochs 3) passed with accuracy 1.0000. Also: --cases suggestions never go below the default 64; train --estimate killed by a signal exits 128+N; docs/diagnostics.md documents the underfit rule and its sample uses base:N ids. Checks: npm run build, lint:node, test:node (89/110/36/8/85), generate-remedies --check, ruff check/format, pytest 672 passed 4 skipped, prettier clean.

Fix round 3 committed as c1368d2 and b4771ca (the hub test no longer imports huggingface_hub, which the dev-only CI job lacks; first CI run 36219685806 failed on that). CI run 36219904056 on b4771ca green on all six jobs.

Finalization validation (on b4771ca): npm run build ok; npm run lint:node clean; npm run test:node compiler 89/89, runtime 110/110, cli 36/36, framework 8/8, benchmarks 85/85; full pytest 672 passed 4 skipped; ruff check + format --check clean; node scripts/generate-remedies.mjs --check exit 0; prettier docs README.md cli/README.md diagnostics/remedies.json clean. AC1 probe (tier program, constraints teacher, USD 0): train --cases 8 --epochs 1 failed with a next: line under each of the gold-miss, constraint and ECE blocks (--epochs 3/--cases 64 underfit advice, --cases 64 for ECE); following it (--cases 64 --epochs 3) passed with accuracy 1.0000. AC2 probes: run with a missing root -> next: semantscript train; current.json {kind:x} -> releases list then rollback <release>, which then ran and returned high; program changed after training -> SemaUnknownFunctionError names build then train; test --bundle on the changed program -> absent function with next: train; --trainer-module semantscript_missing.cli -> one-line 'the trainer stopped: ModuleNotFoundError ...; next: run semantscript doctor ... trainer check; full traceback in ...traceback.txt' (rc 1); --python /nonexistent -> python-missing remedy with the doctor check; HF_HUB_OFFLINE with an empty HF_HOME -> encoder-download-failed remedy. AC3 probe: edited one generated row in docs/diagnostics.md and one in runtime/src/remedies.generated.ts -> generate-remedies --check exit 1 naming both files; restored -> exit 0.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Every train, test and run failure now names the next command. One source, diagnostics/remedies.json, feeds generated modules for the runtime, compiler and trainer and the fix tables in docs/diagnostics.md; scripts/generate-remedies.mjs --check plus node and pytest drift tests catch drift. Train reports end each verification failure with a next: line derived from the evidence: a shared one-field rule becomes a constraint, a repeated miss becomes an example, an underfit head gets more --epochs/--cases, ECE gets --cases or --epochs, and narrow stops get seed-retry flags. The CLI turns trainer tracebacks, missing modules, a missing interpreter and signals into one line naming the doctor check, and the trainer's own errors (missing extra, bad bundle/teacher, OOM, unwritable paths, hub download, option ranges) name a fix. Runtime load errors, stale functions and CLI test/run/build failures name the fixing command. Verified with build, lint, test:node, pytest 672 passed, ruff, prettier, generate --check, and real USD 0 probes of each path (see notes).
<!-- SECTION:FINAL_SUMMARY:END -->
