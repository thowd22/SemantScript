---
id: TASK-14.4
title: >-
  Built-in constraints teacher: train an expression with complete constraints
  and no language model from the CLI
status: Done
assignee:
  - '@claude'
created_date: '2026-09-25 15:21'
updated_date: '2026-09-25 22:26'
labels:
  - dx
  - train
milestone: m-5
dependencies: []
parent_task_id: TASK-14
ordinal: 57000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
When an expression's constraints decide every input, the constraints are a labeling function: the reference application trains all nine of its expressions that way with no API key (examples/refund-service/train.py, a 400-line custom driver), and decision-12 makes constraint labels the local iteration path. But semantscript train has no such backend: [teacher] offers anthropic and ollama only, so a developer who wrote complete constraints still has to pay a teacher or copy the driver. The built-in path should also apply to a mixed expression: constraints label the inputs they decide and the language-model teacher labels the rest.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 [teacher] backend = "constraints" (and semantscript train --teacher constraints) trains any expression whose constraints admit exactly one output for every sampled input, sampling inputs from the IR types with threshold-aware numeric ranges, and generates boundary pairs and counterfactual twins by single-field edits
- [x] #2 An expression whose constraints are incomplete gets a clear message naming an input the constraints do not decide, or, when a language-model teacher is also configured, uses the constraints for the inputs they decide and the teacher for the rest
- [x] #3 examples/refund-service/train.py is replaced by the built-in backend and its tests and README still pass
- [x] #4 The teacher provenance records the constraints teacher and its sampling configuration digest
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Design (from reading teacher_config.py, teacher.py, dataset.py, adversarial.py, constraints.py, case_contract.py, cli.py, doctor.py, cli/src/{defaults,train,doctor,index,init}.ts, examples/refund-service/{train.py,README.md,package.json,test,scripts/measure.mjs}, docs/teachers.md, docs/reference-application.md, decision-12):

1. New module trainer/src/semantscript_trainer/teachers/constraints.py (generalised from examples/refund-service/train.py):
   - ConstraintsTeacherConfig (frozen, closed): backend="constraints", seed=1, twin_filter=true, maximum_sampling_attempts, ranges (per dotted input path or leaf name: low, high, distribution uniform|log|count, decimals), fallback (optional nested LM TeacherConfig table, anthropic|ollama). configuration_sha256 over a public projection (algorithm version, seed, ranges, knobs, fallback descriptor projection; no secrets).
   - ConstraintSampler per IR: compile_constraints; scalar output support via the case-contract head support; per-path numeric thresholds collected from binary comparisons between an input path and a numeric literal (global literals as fallback), inferred ranges (high = max(10, 2*max threshold), count when thresholds <= 10, log when >= 1000, else uniform; TOML ranges override), 30% near-threshold draws; strings from literals compared to that path plus a fixed pool; boolean, enum, literal, union, null, object (optional fields sometimes omitted), tuple, array (0-3 items). label(inputs) -> the single output with no violated constraint via evaluate_output_contract, else Undecided(allowed outputs).
   - ConstraintsTeacher (Teacher + AdversarialTeacher): descriptor provider "constraints", model "semantscript-constraints/v1" (mixed: provider "constraints+<backend>", model "<fallback model>"), digest = sampling config digest. generate(ir,n): distinct decided samples, keeping only inputs with a single-field counterfactual twin when twin_filter; boundary pairs one field apart with the predicate flipped and both sides decided; counterfactual twins by single-field edits (threshold +-1, enum/union/boolean flips). Per-function, per-purpose reproducible RNG streams (seed:id:purpose:attempt) as in the reference.
   - Pure mode, incomplete constraints: TeacherConfigurationError naming the expression (source path:line, id prefix) and one undecided input as JSON with the outputs it admits (none = contradictory constraints, several = incomplete), plus the fix (add constraints or configure [teacher.fallback]). No constraints or a non-scalar output also fails with a clear message in pure mode.
   - Mixed mode (fallback configured, or a Teacher injected for tests): estimate the decided share from a seeded pilot sample; constraints label ceil(share*n) decided samples, the fallback generates the rest, and any fallback case whose input the constraints decide is relabelled by the constraints (they are the authority). Boundary pairs and twins come from constraints when both sides are decided, otherwise from the fallback (skip-able AdversarialGenerationError path when it cannot). Share 1.0 makes no LM request; no constraints delegates entirely.
   - public sample_decided(ir, n, stream) for held-out sets drawn from a stream the training corpus never uses.
2. teacher_config.py: load_teacher_config accepts the keyword "constraints" (built-in default config, no file) and backend = "constraints" tables; create_teacher builds ConstraintsTeacher (with fallback via the existing LM backends). Keep TeacherConfig for LM backends; union return type. Export new names in __init__.py and test_public_api.
3. cli.py: --teacher takes a path or "constraints"; train_bundle fails fast, before generation, when an expression has no gold examples: "<source>:<line> (<id>) has no gold examples; verification needs at least one attested example per expression".
4. doctor.py: constraints backend -> teacher-config pass (describes seed/ranges/fallback), teacher-key pass "needs no key" (or the fallback's key check), teacher-probe skip "makes no request" (or probes the fallback).
5. Node CLI: defaults.ts BUILT_IN_TEACHERS=["constraints"]; findTeacherConfig/resolveTeacherConfig pass the keyword through unless a file with that name exists; help text (index.ts --teacher <teacher.toml|constraints>), init next-steps line; cli tests for keyword pass-through to train and doctor.
6. Tests without GPU: trainer/tests/test_constraints_teacher.py (sampling types, threshold inference, labels, boundary pairs, twins, undecided-input error text, mixed mode with a fake fallback incl. relabelling and share 1.0 making no calls, descriptor digest stability and change on config, TOML loading/closed keys, keyword); test_cli end-to-end train_bundle with the built-in teacher on the complete-constraints fixture using RuleTokenizer/RuleEncoder on CPU (provenance records provider "constraints" + digest), incomplete examples/refund.sem.ts: pure error vs mixed with RuleTeacher fallback; no-gold-example error; doctor tests.
7. examples/refund-service: delete train.py; add semantscript.teacher.toml (backend constraints, ranges from the old NUMBER_RANGES only if inferred ranges underperform); package.json train -> "semantscript train --cases 800 --epochs 12 --batch-size 16 --learning-rate 5e-5 --max-sequence-length 128 --evaluation-ratio 0.1 --select-best-epoch --local-files-only --device cuda --counterfactual-ratio 0.5 --max-constraint-violation-rate 0.005 --application-id refund-service --report .semantscript/train-report.json && npm run heldout"; small scripts/heldout.py writing heldout.json via sample_decided (stream disjoint from training). Run on the GPU (HSA_ENABLE_DXG_DETECTION=1), npm test (3/3 incl. trained-artifact test), npm run measure; update README (train.py references, the teacher section, provenance, results table and release numbers).
8. Docs: docs/teachers.md (constraints backend section, mixed mode TOML, keyword, gold-example note, writing-a-teacher pointer to the built-in module), docs/cli-reference.md (--teacher, doctor rows), docs/reference-application.md, docs/tutorial-refund-decision.md row, examples/README.md, trainer/README.md teacher backends, docs/getting-started.md if it lists backends; prettier --check.
9. Gates: npm run build, lint:node, test:node, ruff check/format, test:python, prettier; commit, push, gh run watch green.

Risks: generic range inference may not reproduce the reference numbers (fallback: explicit ranges in the example TOML); GPU run ~4+ min and possible strict-gate flakiness near thresholds (the recorded 0.005 tolerance stays); mixed-mode semantics are an interpretation (LM invents inputs; the constraints relabel decided ones); changing the example's teacher identity invalidates its caches (full retrain expected).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPLEMENT: added teachers/constraints.py (ConstraintSampler + ConstraintsTeacher, pure and mixed mode, sample_decided), ConstraintsTeacherConfig/NumberRange in teacher_config.py (keyword 'constraints', backend = "constraints" tables with [teacher.ranges] and [teacher.fallback]), doctor checks for the constraints backend, train_bundle gold-example precondition. New trainer/tests/test_constraints_teacher.py (25 tests, CPU only), 4 test_cli cases (built-in teacher end to end with provenance, pure-mode error and mixed mode on examples/refund.sem.ts, no-gold error, --teacher constraints), 1 doctor test; all pass.

IMPLEMENT (cont.): Node CLI passes --teacher constraints through to train, its preflight and doctor (BUILT_IN_TEACHERS in cli/src/defaults.ts; a file of that name still wins); cli test added. examples/refund-service: train.py deleted; npm run train = semantscript train --teacher constraints with the old recipe (800 cases, 12 epochs, select-best-epoch, cfr 0.5, 0.005 tolerance, cuda) && npm run heldout (scripts/heldout.py, heldout stream, cached training inputs excluded); semantscript devDependency file:../../cli. GPU run 2026-09-25 (11 min wall): release 2f9eb3e890d1, all 9 verified (accuracy 1.0000, ECE 0.0000, 0 violations of 14,456 records, selected epoch 3), teacher constraints/compiled-constraints-v1 digest c58c02b8...; inferred ranges sufficed (no teacher TOML). npm test 3/3; npm run measure: 7 of 9 at 200/200, orders.sem.ts:103 and :122 at 199/200 (amounts just past 2,000 and 5,000). Docs updated (teachers, cli-reference, reference-application, tutorial, index, examples/README, trainer/README, refund-service README). Gates: build, lint:node, test:node, ruff check/format, test:python (577 passed), prettier on touched markdown all green. Commit 64c29cd pushed; CI run 36177879923 success.

FIX ROUND 1 (commit 01d9db8): (1) Field-vs-field comparisons: predicates comparing two input paths record peers; number edits add the peer's current value and one step either side; the twin filter and generate_counterfactual now share a per-anchor stream (seed:id:twin:<input>:attempt), so a kept anchor always gets its twin even after a cached base dataset. Reviewer probe (production Synthetic+Adversarial generators, cmp p.x>p.y at 800 cases, seeds 1-10): 10/10 ok (was 0/10). (2) Array length: edits append or remove the last item (one JSON path), strings with length thresholds gain/lose one char; edge bulk items.length>3: 10/10 seeds ok (was 0/10), labels balanced (126/74 at 200). GPU: busy1 via node CLI --teacher constraints --cases 200 train passed (1.0000). cmp at 800 cases/12 epochs with the refund recipe (0.5% tolerance) passed at 0.9937 with 2 violations; the strict gate fails on near-equal pairs (model capacity, not the teacher). (3) Range inference: fractional thresholds -> 0..2*max (score 0.25/0.75 -> 0..1.5), negative-only -> symmetric. (4) CONSTRAINTS_ALGORITHM_VERSION 2 (compiled-constraints-v2). refund-service retrained on GPU: release 1bf655e6e92d, 9/9 verified accuracy 1.0000, ECE <= 0.0026, 0 violations of 14,456 records, selected epoch 4, 11:02 wall; npm test 3/3; measure 5/9 at 200/200 and 4/9 at 199/200 (near-threshold inputs), p50 3.69-5.63 ms; README, reference-application, scaling-results, tutorial, teachers updated. (5) Docs: getting-started (constraints path, gold examples in triage), diagnostics (gold-example row, constraints teacher errors, --teacher constraints fixes), environment (teacher rows), components, cli/README; init starter now has two gold examples. (6) Advisory: a directory named constraints no longer shadows the keyword (Node and Python); unknown backend lists anthropic, ollama or constraints; doctor omits mode for Ollama and its no-teacher fix suggests --teacher constraints; measure.mjs comment and README npm install comment fixed. Gates: build, lint:node, test:node (all 0 failures), ruff check/format, test:python 585 passed 4 skipped, prettier on docs clean.

FIX ROUND 2 (commit a0ec181, CI 36189744718 success): (1) Range inference crash: min()/max() with starred args became one-argument calls for a number with no negative threshold and no gold value (optional field left out of examples, items of an empty example array, an uncovered union variant); now list-based. Reproduced first: scratch project opt via node CLI printed "error: 'float' object is not iterable"; after the fix train --teacher constraints --cases 200 --epochs 6 passed (1.0000). (2) Arithmetic predicates: a path compared with no literal of its own gets at least 0..100 (was 0..30 for a - b > 10, so 800 cases repeated anchors); a repeated anchor's counterfactual falls back to the twin filter's attempt-0 stream. Reproduced first: scratch gap at 800 cases failed 'only 789 of 798 counterfactual pairs'; after: generation OK (1602 adversarial), strict gate 0.9842 with 9 violations (model capacity), and with 12 epochs, select-best-epoch, 0.005 tolerance train passed 0.9960. refund-service ranges and 200-case corpora are byte-identical before and after (snapshot hash per expression), so the algorithm version stays v2 and release 1bf655e6e92d stays valid. (3) Mixed mode: fallback boundary pairs and twins are checked in the teacher and a bad one raises an error naming the expression and the fallback (provider/model); whole-number floats from the fallback become integers; the decided top-up repeats cases when the input space is exhausted (was 'found 0 of 88' error). Real run: route(ticket) with qwen3:14b fallback, --cases 160 --epochs 10 --select-best-epoch --counterfactual-ratio 0: train passed (0.9444), provenance constraints+ollama/qwen3:14b; with the default counterfactual ratio it still fails (fallback twins decided back to the anchor's output), now with the named error. docs/teachers.md and trainer/README.md say so. (4) docs/reference-application.md provenance excerpt now constraints/compiled-constraints-v2@sha256:954871d5. Advisory fixes: twin-filter error wording and how to write the TOML, invalid gold example names the expression, doctor suggests --teacher constraints on a missing file, architecture node, getting-started typo, README gold-example note, diagnostics rows, arithmetic/union caveats in teachers.md, refund-service latency from one dated measure run (21:06 UTC). Tests: 11 new cases in test_constraints_teacher.py, all fail on the previous constraints.py and pass now. Gates: build, lint:node, lint:python, ruff format --check, test:python 596 passed 4 skipped, test:node 0 failures, refund-service npm test 3/3, prettier clean.

FIX ROUND 3 (commit 8af2107, CI 36195385017 success): (1) Zero thresholds: reproduced first via production generators (sign: balance<0/>=0 'found 0 of 63 ... 251 decided, 251 with no such edit'; pos: amount>0/<=0 range 0..1, 2 distinct inputs, 1 split group). Range inference now makes a path symmetric around zero (at least -10..10) when a threshold is negative or a comparison holds at or below zero (x < c, x <= c with c <= 0); a count compared only as > 0 / === 0 keeps 0..N (refund chargebacks unchanged in kind, now 0..10 count instead of 0..1). (2) Single split group: reproduced first (route severity>=8 + vip: 34 distinct inputs, 1 group at 64/200/800 cases). Counterfactual twins now try SPREAD_DRAWS=6 number values drawn evenly over the range before threshold-adjacent ones (boundary pairs unchanged); an expression whose seeded 2,000-draw pilot has fewer than 800 distinct inputs is compact and draws whole-number ranges with two decimals half the time (refund-service expressions are not compact: 1144-1999 of 2000). After: route 63/64, 198/200, 798/800 distinct with 39/110/356 groups; sign/pos/gt0/gt7 all split. Node CLI on GPU, scratch projects at defaults (64 cases, ratio 1, 8 epochs, select-best-epoch): route, sign and pos each train passed 1.0000, 0 violations. The row-group error now names <source>:<line> (<id>), row and distinct-input counts and the fixes (--counterfactual-ratio 0.5/0, fewer --cases, [teacher.ranges]); train warns when fewer than half the cases are distinct. Two booleans still collapse and get that message. Advisory: incomplete/no-constraints errors say how to write the [teacher.fallback] TOML and pass --teacher <file>; unknown keys list the valid ones; maximum_sampling_attempts documented; cli-reference teacher-config row mentions the keyword; diagnostics rows for the new messages; teachers.md small-input-space section. Algorithm v3 (compiled-constraints-v3, default digest 9ce05167...). refund-service retrained on GPU with npm run train (about 8 min wall, 22:00:35-22:08:14 UTC): release 11c6378e9e9d, 9/9 verified, 8 at accuracy 1.0000 and orders.sem.ts:103 at 0.9938, ECE <= 0.0014, 1 raw violation of 14,456 records (within the 0.5% tolerance), selected epoch 4; npm test 3/3; npm run measure (22:09 UTC) 8/9 at 200/200, orders.sem.ts:103 199/200 (total 2011), p50 3.84-5.39 ms; README, reference-application, scaling-results, tutorial updated. Tests: 8 new cases in test_constraints_teacher.py (7 fail on the previous code; the count-range regression guard passes on both); 2 old tests adjusted (celsius range now -10..10; repeated-anchor test uses two booleans, since a small numeric range is now compact). Gates: npm run build, lint:node, ruff check, ruff format --check (61 files), test:python 604 passed 4 skipped, test:node 89/105/18/8/85 0 failures, prettier clean.

FINALIZE (2026-09-25): re-verified independently. Fresh scratch projects via node CLI on the GPU, train --teacher constraints --epochs 8 --select-best-epoch at the default 64 cases and ratio 1: sign (balance < 0 / >= 0, the round-3 blocker) passed 1.0000, 0 violations, release 6660e16d; route (severity >= 8 + vip, the other blocker) passed 1.0000, 0 violations, release 9505b3a1. pos (amount > 0 / <= 0) alone at those defaults generated fine (5 of 128 adversarial and 2 of 64 cases at exactly 0) but failed the strict gate: the model predicted charge at amount 0 (gold + 7 adversarial records); with --cases 200 --epochs 12 it passed 1.0000, 0 violations (release 99759962). That is a model-budget limit on a single-point class, not a teacher defect. Incomplete constraints (only balance < 0) via CLI: error names src/balance.sem.ts:4 (nf_f630bc8f), input {"account":{"balance":0}} admitting ok and overdrawn, and the [teacher.fallback] TOML fix. Mixed mode: 7 mixed-mode tests plus test_incomplete_constraints_name_an_input_or_train_with_a_fallback_teacher pass. examples/refund-service: train.py absent; npm run train exit 0 (build cache reused all 9 under the current teacher digest, release 11c6378e stands, heldout.json 1800 cases); npm test 3/3; release manifest trainingProvenance.teacher = constraints/compiled-constraints-v3@sha256:9ce05167..., equal to create_teacher(load_teacher_config('constraints')).descriptor. Gates: npm run build 0, lint:node 0, ruff check clean, ruff format --check 61 files, prettier --check docs and READMEs clean, test:node 89/105/18/8/85 pass 0 fail, test:python 604 passed 4 skipped.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added a built-in constraints teacher (trainer/src/semantscript_trainer/teachers/constraints.py; [teacher] backend = "constraints" or --teacher constraints). It samples inputs from IR types with threshold-aware numeric ranges (symmetric around zero for negative or at-or-below-zero comparisons, decimals for compact input spaces), labels by compiled constraints, keeps inputs with a single-field twin, and builds boundary pairs and spread counterfactual twins by single-field edits. Incomplete constraints fail with the expression and an undecided input, or train in mixed mode with a [teacher.fallback] language-model teacher. Provenance records constraints/compiled-constraints-v3 and the sampling configuration digest. examples/refund-service/train.py is gone; npm run train uses the built-in backend (release 11c6378e, 9/9 verified, npm test 3/3). Also: gold-example precondition, clearer row-group error, doctor and Node CLI support, docs. Verified with GPU runs through the node CLI (sign, route, pos, refund-service), CLI error check, test:python 604 passed, test:node 0 failures, lint, ruff and prettier clean.
<!-- SECTION:FINAL_SUMMARY:END -->
