---
id: TASK-14.5
title: 'Teacher onboarding, cost estimate and spend cap for semantscript train'
status: In Progress
assignee:
  - '@claude'
created_date: '2026-09-25 15:21'
updated_date: '2026-09-25 23:31'
labels:
  - dx
  - train
milestone: m-5
dependencies: []
parent_task_id: TASK-14
ordinal: 58000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
A training run through a paid teacher gives the developer no idea what it will cost or how long it will take, and no way to bound it. On 2026-09-25 the Express example's two expressions at 192 cases cost about USD 10 through OpenRouter against an estimate of one dollar, because the teacher prompt is about 8,300 tokens per request, a constrained expression needs about twice as many requests as cases, and an anchor the teacher cannot twin costs three attempts. Setting the teacher up is also manual: init assumes ANTHROPIC_API_KEY and anything else means writing a TOML by hand from the trainer README.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 semantscript train --estimate prints, per expression and in total, the number of teacher requests, the input and output tokens, the cost in USD at the configured backend's price and the expected wall time, without calling the teacher
- [ ] #2 --max-cost-usd stops a run before the request that would exceed it, with the cached datasets kept, and every run prints its running cost and request count
- [ ] #3 init asks for or accepts a teacher choice (Anthropic, OpenRouter, Ollama, constraints) and writes the TOML; semantscript teacher probe sends one request through the configured backend and reports the model, latency and cost
- [ ] #4 The teacher prompt is measured and reduced (or served through prompt caching) so that a case costs no more than half of the 2026-09-25 figure at equal verification results on the Express example
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Measured baseline (UNDERSTAND, local, free): the Express IR's decideRefund case prompt averages 20,434 characters (system 929; user = indented JSON with the full constraint predicate ASTs, 6,070 chars of constraints, plus a responseSchema copy that output_config already sends); triage averages 3,815. 8,300 tokens / 20,434 chars ~ 2.46 chars per token for this indented JSON. A compact projection (constraints as kind/output/source only, no responseSchema copy, no indent, inputs without index) is ~4,470 chars for decideRefund (-78%) and ~2,390 for triage. OpenRouter GET /api/v1/models (no key) gives anthropic/claude-sonnet-5 prompt 2e-6, completion 1e-5, input_cache_read 2e-7, input_cache_write 2.5e-6 and ':batch' variants at half; 8,300 x 2e-6 + ~80 x 1e-5 = USD 0.0174, matching the recorded 0.017. Half of the 2026-09-25 figure = USD 0.0085 per request.

1. Spend accounting (new trainer/src/semantscript_trainer/teacher_spend.py): TeacherPrice (input/output/cache-read/cache-write USD per million, source pinned|openrouter|config|free); pinned table for Anthropic ids (claude-sonnet-5 2/10, claude-opus-5 5/25, claude-haiku-4-5 1/5; cache read 0.1x, write 1.25x; batch 0.5x); resolve_price(config): constraints and ollama -> zero; anthropic with an openrouter.ai base_url -> OpenRouter /api/v1/models (cached in <cache-dir>/teacher-prices.json with fetch date, pinned fallback offline), else the pinned table; [teacher.pricing] (input_usd_per_million, output_usd_per_million, cache_read_usd_per_million, cache_write_usd_per_million, seconds_per_request) overrides; unknown model with no override -> typed TeacherConfigurationError naming [teacher.pricing]. Pricing is excluded from public_projection so it never changes dataset digests. SpendMeter: requests, input/output/cache tokens, USD, elapsed; charge(usage) from Anthropic usage (input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens) and Ollama usage (prompt/completion tokens, zero cost); reserve(estimated_usd) raises TeacherBudgetExceeded (new TeacherError subclass) before a request when spent + estimate > max_cost_usd; progress line every 25 requests and per expression.
2. Wire the meter into AnthropicTeacher (_generate_direct, _request_adversarial: reserve before each create, charge after; submit_batch: reserve the whole batch at the batch price before submitting, charge on collect) and OllamaTeacher (count, latency, zero cost) through a provider option meter=; ConstraintsTeacher passes it to its fallback; no meter = current behavior. Request estimate for reserve() = the request's own prompt characters / chars-per-token ratio + max(observed mean output, pinned 110).
3. Response journal so a stopped run loses no paid work: <cache-dir>/teacher-responses/<descriptor digest>/<sha256(request params)>-<occurrence n>.json written after each successful direct request and replayed (charged USD 0) on rerun; occurrence counting keeps retries of an identical prompt distinct; complete datasets stay in the existing dataset/adversarial caches. Datasets finished before a stop are never deleted.
4. Estimate (new trainer/src/semantscript_trainer/teacher_estimate.py, no teacher call, no torch/tokenizer load): per function, skip build-cache reused functions and datasets whose cache file exists (SyntheticDatasetGenerator.cache_path / AdversarialDatasetGenerator.cache_path when the base is cached); requests = synthetic (cases - gold) + one per constraint (boundary) + ratio_count(synthetic, counterfactual_ratio) (twins), expected with a documented retry factor calibrated on the Express run (~600 requests for the planned ~480) and a maximum (4 replacement rounds, 3 attempts per boundary/twin, one skip per wanted pair); input tokens = characters of the exact prompts built by build_case_messages/build_boundary_messages/build_counterfactual_messages (anchor = a gold example) plus the wire schema, divided by a documented chars-per-token ratio (calibrated from the usage of the probe requests in step 9; stated in output and docs); output tokens = serialized case size / ratio (+ reason for twins); USD at the resolved price (cache reads when caching applies, batch price for mode batch/auto above batch_threshold); wall time = requests x seconds_per_request (config override, else the mean latency the last metered run recorded in <cache-dir>/teacher-stats.json for this descriptor, else pinned: Sonnet direct 4 s, Ollama 1 s documented as a pinned figure, constraints 0; batch printed as up to 24 h). Output kind semantscript.train-estimate v1 (per function and total: requests expected/max, input/output tokens, USD, seconds; price source; ratio).
5. Trainer CLI (cli.py): train --estimate (writes the estimate JSON to stdout, exits 0, never creates the teacher client), --max-cost-usd <x> (positive finite; meter cap), teacher probe subcommand (--teacher, --json: one request via doctor.probe_teacher, reports model, latency, tokens, USD at the resolved price; constraints -> 'no request, USD 0'; mixed -> probes the fallback). Train report gains a teacher spend object {requests, replayed, inputTokens, outputTokens, cacheReadTokens, costUsd, maxCostUsd} (REPORT_VERSION stays 1: additive, Node parser tolerant) and the stderr log prints running cost/request lines; TeacherBudgetExceeded is caught with 'error: spend cap USD x reached after n requests (USD y spent); completed datasets and n journaled responses are cached; rerun with a higher --max-cost-usd to resume' and exit 1.
6. Prompt reduction (AC4): teacher_prompt.build_case_messages and adversarial_prompt send a compact contract: constraints as {kind, output, source} (no predicate AST; the local checks still use it), inputs as {name, type, tsType}, no responseSchema copy (both Anthropic output_config and Ollama response_format carry the schema), compact JSON; the stable contract goes first as a system block with cache_control ephemeral (prompt caching through Anthropic and OpenRouter's Anthropic route; below the model's minimum cacheable length it is simply not cached), the per-case brief in the user message. To avoid invalidating the constraints teacher's caches (dataset sha includes the request key; bumping PROMPT_CONTRACT_VERSION would force a refund-service retrain), add promptContractVersion (case and adversarial) to TeacherConfig.public_projection so only language-model teacher digests change; document the old-cache invalidation for Anthropic/Ollama users. Update test_teacher_prompt/test_anthropic_teacher/test_ollama_teacher expectations.
7. Node CLI: train.ts/dev pass --estimate and --max-cost-usd; --estimate skips the heavy preflight (teacher-config check only), captures stdout and renders a per-expression table (requests, input tokens, output tokens, USD, time) plus total and price source; renderTrainReport prints 'teacher: n requests (m replayed), USD x (cap y)'. New cli/src/teacher.ts: semantscript teacher probe [--teacher] [--python] [--json] rendering model, latency, tokens, cost. init: --teacher anthropic|openrouter|ollama|constraints [--teacher-model], an interactive question only when stdin is a TTY and no flag (CliIo gains optional ask()), writes .semantscript/teacher.toml from templates in defaults.ts (no key; OpenRouter template = anthropic backend, anthropic/claude-sonnet-5, base_url https://openrouter.ai/api, mode direct, max_tokens 4096 with a comment to export ANTHROPIC_API_KEY=$OPENROUTER_API_KEY), never overwrites an existing teacher file (reports unchanged), then runs its doctor with that teacher. USAGE, index exports.
8. Tests beside features: trainer/tests/test_teacher_spend.py (price resolution incl. OpenRouter fetch via injected fetcher and offline fallback, overrides, unknown model error, meter charge/reserve/cap raising before the request, digests unchanged by pricing), test_teacher_estimate.py (Express IR fixture: request counts, zero for constraints, zero for cached datasets, no teacher call), test_anthropic_teacher (meter wiring with FakeClient usage, cap stops before the Nth request, batch reserve, journal replay charges nothing and preserves retries), test_cli (--estimate, --max-cost-usd stop keeps cached datasets and resumes, teacher probe with fake client, report spend object), test_doctor unchanged paths. cli/test/cli.test.mjs + fake_trainer.py: estimate rendering, --max-cost-usd pass-through, teacher probe command, init --teacher each choice writes TOML without a key and is idempotent, spend line in train report.
9. Measurements (paid, capped far below USD 0.50): try OpenRouter's count_tokens route (free) else one real request with the old Express decideRefund prompt and two with the new (second shows cache_read), key from .env via environment only, never printed: record usage.input_tokens before/after and USD; `semantscript teacher probe` through OpenRouter and Ollama (qwen3:14b), and --teacher constraints (USD 0); `semantscript train --estimate` on examples/express-app with the OpenRouter TOML (192 cases, cfr 0.5) and with constraints; a small --max-cost-usd run (e.g. cap 0.05 on a scratch copy) proving the stop, the cached datasets and a zero-cost resume via the journal. AC4 full comparison (regenerate both Express datasets with the reduced prompt, ~600 requests at ~USD 0.004-0.005 = about USD 3, then the same train recipe and seed) exceeds the 0.50 budget: report its exact estimate from --estimate and leave AC4 unchecked unless approved.
10. Docs: docs/teachers.md (estimate, cap, pricing table/override, OpenRouter price source, journal, prompt reduction numbers and caching, init choices, teacher probe), docs/cli-reference.md (init --teacher, train --estimate/--max-cost-usd, teacher probe), docs/getting-started.md (choose a teacher at init, estimate before the first paid run), docs/diagnostics.md (spend cap, unknown price, estimate rows), docs/build-cache.md (response journal and teacher-prices/stats files), README.md and cli/README.md command lists, trainer/README.md, examples/express-app/README.md (estimate for its recipe, measured reduced prompt). npx prettier --check docs README.md.
11. Gates: npm run build, npm run lint:node, npm run test:node, ruff check + format --check, Python tests, prettier; commit with the attribution trailers, push, gh run watch green.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPLEMENT progress (2026-09-25):
- Prompt reduction: teacher_prompt/adversarial_prompt now send a compact layout (TEACHER_PROMPT_VERSION 4): stable contract (template, examples, constraints as index/kind/output/source without predicate AST, inputs without index, output) appended to the system prompt as compact JSON; user message = case brief / selected constraint / anchor; no responseSchema copy. PROMPT_CONTRACT_VERSION stays 3 (dataset digest, constraints teacher unchanged); promptVersion enters TeacherConfig.public_projection so only language-model caches regenerate. Anthropic sends the system as one cache_control ephemeral block.
- Local measurement (free): Express decideRefund case prompt 20,433 -> 4,799 chars (-77%), boundary 23,641 -> 3,727, counterfactual 19,737 -> 3,606; triage case 3,816 -> 2,527.
- Paid probe through OpenRouter (anthropic/claude-sonnet-5, thinking disabled; 3 requests, USD 0.0226 total, key read from .env into the process only): old prompt case 5 of 189 = 6,552 input / 68 output tokens, USD 0.013784; new prompt same case = 264 uncached + 2,347 cache-write input / 68 output, USD 0.0070755; next case = 292 uncached + 2,347 cache-read / 66 output, USD 0.0017134 (OpenRouter usage.cost matches the meter's computed cost exactly). Half of the recorded USD 0.017 per request is 0.0085: new first request 0.0071, cached requests 0.0017.
- Calibrated CHARACTERS_PER_TOKEN = 2.1 for the compact prompts (5,468 chars -> 2,611 tokens).
- New teacher_spend.py (prices: pinned Anthropic table, OpenRouter /api/v1/models cached a day, [teacher.pricing] override, TeacherPriceUnknown; SpendMeter with cap -> TeacherBudgetExceeded before the request; ResponseJournal replay), teacher_estimate.py (semantscript.train-estimate v1), trainer cli: train --estimate, --max-cost-usd, teacher probe.
- Express estimate with the reduced prompt (192 cases, cfr 0.5, OpenRouter price list): 596 expected requests (1,533 max), USD 1.02 expected (2.88 max), about 40 min direct.

IMPLEMENT complete (2026-09-25), uncommitted before the CI push:
- Node CLI: train/dev --estimate (skips preflight, renders per-expression table + total + cap verdict) and --max-cost-usd (validated, passed through); report table ends with 'teacher: n requests (m replayed), USD x of the USD cap'; new 'semantscript teacher probe' (cli/src/teacher.ts); init --teacher anthropic|openrouter|ollama|constraints [--teacher-model], asks via CliIo.ask only on a TTY, writes .semantscript/teacher.toml from templates with no key, never replaces an existing teacher file, runs its doctor against that file.
- Spend cap reservation made conservative after the first live capped run overshot (USD 0.0223 on a 0.02 cap): each request is now reserved at its prompt tokens x1.2 at the cache-write price plus the largest output seen x1.25; the second live capped run stopped at USD 0.0186 under the 0.02 cap.
- Live evidence (OpenRouter, key from .env via env only): teacher probe 2.29 s, 16 in / 4 out, USD 0.000072; Ollama qwen3:14b probe 8.33 s (model load), 6 cases per Express expression at about 1.0 s each (pinned Ollama seconds_per_request = 1.0); constraints probe sends nothing, USD 0. Capped scratch run on the Express bundle (--cases 8, cfr 0.5): run 1 cap 0.02 stopped after 6 paid requests, decideRefund base dataset cached, 6 journal entries; run 2 same cap replayed 1 and paid 6 more, stopped at USD 0.0186; run 3 cap 0.10 replayed 7, paid 7 (USD 0.0194) and completed all datasets (tiny model then failed verification, as expected at 8 cases/1 epoch). Estimate for that run: 26 expected requests, USD 0.06; actual 19 paid, USD 0.060.
- Total OpenRouter spend in this task: about USD 0.083.
- Also adapted benchmarks/refund/program/test_generate_training_corpus.py stub to the compact user messages.
- Docs: teachers.md (choosing, probe, prompt size and caching, estimate and cap, pricing), cli-reference.md, getting-started.md, diagnostics.md, build-cache.md, index.md, components.md, tutorial-refund-decision.md, README.md, cli/README.md, trainer/README.md, examples/express-app/README.md.
- Gates: npm run build ok; npm run lint:node ok; npm run test:node all pass (89/105/22/8/85); npm run test:python 630 passed 4 skipped; npm run lint:python ok; prettier --check docs README.md ok.
- AC4 open: equal verification not measured; full comparison estimated USD 1.02 expected (max 2.88) + GPU retrain, above the USD 0.50 budget.

Committed 205dbe6 (TASK-14.5: teacher cost estimate, spend cap, probe, init teacher choice, compact cached prompt), pushed to origin main; CI run 36199729866 success on every job (Node lint/build/tests, Python dev and dev+training, fresh clone/examples/Docker, doctor on Windows and macOS).

FIX round 1 (2026-09-25):
- Journal no longer replays rejected answers: AnthropicTeacher drops the journal entry of a response that fails decoding/contract checks (case, boundary, counterfactual); train drops every entry the run replayed or wrote when it fails on rejected teacher answers (TeacherResponseError, AdversarialGenerationError, ConstraintError, DatasetError in the cause chain; not on transport errors, the spend cap or verification) and prints a note. Reviewer's journal_poison.py now gives 'run 2: ok, sent 2'. Fake-Anthropic end-to-end rerun of the boundary-pair failure on the reviewer's cacheB copy: run 1 replayed 3 and discarded them; run 2 sent 3 fresh requests (server log 0 -> 3).
- Ollama backend now reserves each request against --max-cost-usd (priced endpoint via [teacher.pricing]); live run with ollama-priced.toml cap 0.05 stops before the first request (reservation USD 0.389). Journal is created only for the anthropic backend.
- teacher probe runs the doctor's teacher-key check first: missing key -> 'no request sent: ANTHROPIC_API_KEY is not set; OPENROUTER_API_KEY is ...' with the export fix, requestSent false, USD 0; ProbeResult.request_sent replaces the latency heuristic. --json output carries kind/probeVersion; constraints price source no longer doubled.
- Advisory fixes: docs state the cap can be passed by one unusually long answer; build-cache/diagnostics/trainer README describe journal dropping and that batch results are not journaled; old USD 0.60 and USD 0.003-per-case figures replaced with --estimate figures; train's no-teacher error mentions init --teacher.
- AC4 still unverified (paid comparison USD 1.02 expected / 2.88 max needs user approval).
- Gates: build ok, lint:node ok, test:node all pass, test:python 634 passed 4 skipped, ruff ok, prettier ok.
<!-- SECTION:NOTES:END -->
