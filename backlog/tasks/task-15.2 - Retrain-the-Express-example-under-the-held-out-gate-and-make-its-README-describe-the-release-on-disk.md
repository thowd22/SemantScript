---
id: TASK-15.2
title: >-
  Retrain the Express example under the held-out gate and make its README
  describe the release on disk
status: In Progress
assignee:
  - '@claude'
created_date: '2026-09-26 06:27'
updated_date: '2026-09-26 18:16'
labels:
  - dx
  - example
milestone: m-6
dependencies:
  - TASK-15.1
parent_task_id: TASK-15
priority: high
ordinal: 67000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
examples/express-app currently serves release 217d386c (built 2026-09-26T00:48Z, seed 5 of the TASK-14.5 reduced-prompt comparison, refund accuracy 0.924, ECE 0.072), which violates the 90-day rule on stale orders, while examples/express-app/README.md quotes release 5c755d08 (refund 0.987, ECE 0.008) that is no longer under .semantscript/artifact/releases. The cached datasets (examples/express-app/.semantscript/cache: the reduced-prompt dataset caa8e895… and two earlier ones) allow retraining without teacher spend; seed retry (--seed-attempts) is available. Do not spend more than USD 0.50 of the OpenRouter key without the user's approval; if no seed passes from the cache, record the per-seed held-out violation rates and stop. Held-out benchmark inputs never enter training. Related: docs/tutorial-refund-decision.md and docs/deploy.md quote the example's sizes and numbers.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 examples/express-app serves a release trained from the cached datasets that passes the held-out constraint gate, and semantscript explain answers deny for standard and enterprise customers with paid and fraudulent orders at 100, 120, 150 and 200 days
- [ ] #2 examples/express-app/README.md, docs/tutorial-refund-decision.md and docs/deploy.md quote the current release's digest, seed, accuracy, ECE, corpus and held-out violation figures and sizes, and semantscript releases list agrees with them
- [x] #3 If no cached-dataset seed passes, the per-seed corpus and held-out violation rates are recorded in the task and no teacher spend happens without the user's approval
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Resumed 2026-09-26 with the user's spend approval (OpenRouter key up to its remaining balance, ~USD 32; 'this needs to work with express'). Goal: a release of examples/express-app that passes the held-out constraint gate (1% of 512, decision-8, unchanged) and denies on the 16-input grid. AC1's 'trained from the cached datasets' wording cannot be met (27 cached runs failed, AC3); the new release is teacher-generated under the user's approval and the notes/docs say so.

Findings (read 2026-09-26): src/refunds.sem.ts has six constraints; they decide every paid/fraudulent input and every input with ageDays > 90, and leave cancelled orders at <= 90 days open (fallback). src/triage.sem.ts has no constraints, so a [teacher.fallback] build sends triage entirely to the fallback (triage dataset regenerates; it was included in the USD 0.39 estimate). The held-out sampler (held_out.py) uses the constraints sampler's own inferred ranges (ageDays 0..180, twice the largest threshold), excludes every corpus row, gold example and attested case, and is seeded by the build's first --seed; so a constraints-labelled corpus matches the gate's distribution, but the default training range stops at 180 and the AC1 grid includes 200 days. docs/teachers.md: an Anthropic fallback has not been run in mixed mode; local fallbacks failed on counterfactual twins at ratio 1. The example has no node_modules in the worktree (npm install needed, it is not a workspace).

1. Setup (USD 0): cp -a /home/admin2/SemantScript/examples/express-app/.semantscript into the worktree's examples/express-app (1.9 GB, git-ignored; main checkout untouched); npm install && npm run build in examples/express-app. Write .semantscript/teacher-constraints.toml: [teacher] backend = "constraints" (seed 1, defaults), [teacher.ranges] "order.ageDays" = { low = 0, high = 240 } so training covers the 200-day grid point, [teacher.fallback] = the existing OpenRouter Sonnet 5 table (backend anthropic, model anthropic/claude-sonnet-5, base_url https://openrouter.ai/api, mode direct, max_tokens 4096). Key: export ANTHROPIC_API_KEY from .env's OPENROUTER_API_KEY inside the command only, never echoed.
2. Probe: semantscript teacher probe --teacher <toml> (fallback probe, < USD 0.001) and semantscript train --estimate with --cases 384 --counterfactual-ratio 0.5 (and 192 for comparison); quote both in the notes before any paid run.
3. Run (c): npx semantscript train --teacher .semantscript/teacher-constraints.toml --cases 384 --epochs 16 --select-best-epoch --counterfactual-ratio 0.5 --max-constraint-violation-rate 0.01 --seed 1 --seed-attempts 3 --device cuda --max-cost-usd <= 12 --report .semantscript/train-report-15.2-c384-seed1.json (HSA_ENABLE_DXG_DETECTION=1). Record teacher requests and USD from the report in the notes. If counterfactual twins from the fallback fail (the mixed-mode risk), retry with --counterfactual-ratio 0.25 then 0, then fall back to --cases 192.
4. If the gate fails: retrain from the now-cached constraints datasets (USD 0) over seeds 2..10 and epochs 16/24 with --select-best-epoch; then add gold examples for stale paid and fraudulent orders on both tiers (e.g. standard/paid/150 deny, enterprise/fraudulent/120 deny, enterprise/paid/200 deny, standard/fraudulent/100 deny) to src/refunds.sem.ts keeping the existing three (refund dataset regenerates; estimate, then run), then --cases 768 with --seed-attempts; each paid run capped at USD 12, stop when the key's budget is exhausted; record per-run accuracy, ECE, corpus and held-out figures and cost.
5. When a release passes: semantscript explain dist/refunds.sem.js --call decideRefund over standard/enterprise x paid/fraudulent x 100/120/150/200 days (16 inputs), all deny; semantscript test passes; semantscript releases list/show agree (digest, seed, accuracy, ECE, held-out x/512). Sizes: semantscript package --target lambda-zip; releases derive --int8 (--per-channel, tolerance as needed) on the new release, package via promote, then promote the float32 release back to current. Run the example tests (npm test in examples/express-app) and the packaged handler smoke.
6. Docs (AC2): examples/express-app/README.md (release on disk: digest, seed, recipe incl. teacher-constraints.toml contents, accuracy, ECE, corpus x/N, held-out x/512, triage figures, teacher cost, sizes and int8 table; 217d386c and the 27-run sweep condensed into history; quick-start train line and 'gate not yet passed' wording updated), docs/tutorial-refund-decision.md steps 4/5 and 'What you have', docs/deploy.md worked case and int8 table, docs/teachers.md (first Anthropic fallback run in mixed mode: what happened), docs/training-pipeline.md one line. npx prettier --write on touched markdown; prettier --check docs README.md examples/express-app/README.md.
7. Checks: npm run lint:node, npm test -w cli, trainer pytest if Python is touched (it should not be). Commit on task-15.2 with the attribution trailer, git push -u origin task-15.2, gh run watch until green. Finalize per task-finalization: AC1 and AC2 checked with explain/releases evidence (AC1 noting the dataset is teacher-generated with approval, not cached).
8. Result for the orchestrator: worktree paths examples/express-app/.semantscript/artifact, .semantscript/cache, the train/derive reports and teacher-constraints.toml to copy back into the main checkout.

Risks: Anthropic fallback in mixed mode is untested (twins/boundary pairs for cancelled orders may fail; mitigate by lowering --counterfactual-ratio); the gate may still fail with a constraints corpus (then gold examples and 768 cases); widening ageDays to 240 moves training away from the gate's 0..180 distribution slightly; GPU training is not bit-for-bit repeatable; choosing among seeds also chooses held-out samples, so the 16-input explain grid is the independent check; AC1 wording says 'cached datasets' and the orchestrator/user must accept the teacher-generated release as meeting it; ~2 GB disk for the .semantscript copy; parallel tasks may edit docs/ (merge conflicts).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Seed sweep 2026-09-26 (RX 9070 XT, cached datasets caa8e895/fde84ed3 + triage 2cbfbe07, recipe --cases 192 --epochs 8 --select-best-epoch --counterfactual-ratio 0.5 --max-constraint-violation-rate 0.01 --seed N --seed-attempts 1, dummy key + --max-cost-usd 0.01; every run 0 teacher requests, USD 0). decideRefund per seed (accuracy, ECE, corpus violations of 394, held-out inputs broken of 512 [per constraint]); triage passed at 1.000 every seed:
seed 1: 0.9747, 0.0227, 1/394, 60/512 (11.7%) [c0 39, c2 4, c3 14, c5 3]
seed 2: 0.9750, 0.0414, 6/394, 28/512 (5.5%) [c0 2, c2 8, c3 11, c4 2, c5 5]
seed 3: 0.9873, 0.0106, 5/394, 70/512 (13.7%) [c0 25, c2 12, c3 18, c4 11, c5 4]
seed 4: 0.9620, 0.0302, 8/394, 53/512 (10.4%) [c0 33, c3 15, c5 5]
seed 5: 0.9494, 0.0620, 8/394, 109/512 (21.3%) [c0 81, c2 3, c3 19, c5 6]
seed 6: 0.9625, 0.0122, 0/394, 30/512 (5.9%) [c0 15, c2 5, c3 6, c4 2, c5 2]
seed 7: 0.9367, 0.0703, 12/394, 72/512 (14.1%) [c0 50, c2 3, c3 13, c4 1, c5 5]
seed 8: 0.9231, 0.0290, 8/394, 60/512 (11.7%) [c0 25, c2 6, c3 18, c4 2, c5 9]
seed 9: 0.9750, 0.0366, 5/394, 50/512 (9.8%) [c0 17, c2 8, c3 20, c4 3, c5 2]
seed 10: 0.9351, 0.0876, 1/394, 43/512 (8.4%) [c0 7, c2 19, c3 12, c4 3, c5 2]
No seed passes the held-out gate (best 28/512 = 5.5%, gate 1%); the violations are spread over constraints 0, 2, 3, 4 and 5, not only the 90-day rule. Next: training-only variants from the same cache.

Training-only variants from the same cache (0 teacher requests, USD 0 each), decideRefund accuracy / ECE / corpus of 394 / held-out of 512:
seed 2: --epochs 16 (last epoch) 0.9625/0.0306/1/25; --epochs 16 --select-best-epoch 0.9875/0.0225/2/20 (3.9%, best of all runs); --epochs 32 --select-best-epoch 0.9875/0.0225/2/20 (same epoch 12); --learning-rate 5e-5 0.9625/0.0450/9/60; --epochs 16 --learning-rate 1e-5 0.9750/0.0517/2/34; --head-architecture mlp 0.9750/0.0371/6/43; --epochs 8 without --select-best-epoch 0.9000/0.0770/5/49; --epochs 16 --batch-size 4 --select-best-epoch 0.9875/0.0214/0/38.
seed 6: --epochs 16 0.9500/0.0276/0/28; --epochs 16/32 --select-best-epoch 0.9625/0.0122/0/30 (epoch 7, as the base run); --learning-rate 5e-5 0.9500/0.0569/2/37; --epochs 16 --learning-rate 1e-5 0.9625/0.0202/0/35; mlp 0.9500/0.0493/6/77; no select 0.9375/0.0343/2/35.
seed 1: --epochs 32 --select-best-epoch 0.9873/0.0261/0/67. seed 9: --epochs 16 --select-best-epoch 0.9750/0.0241/6/65.
No cached-dataset run passes the 1% held-out gate (best 20/512 = 3.9%); tolerance and sample size left at 1% / 512. The release on disk, 217d386c, answers approve for paid and review for fraudulent orders at 100/120/150/200 days for both tiers (semantscript run, 0 of 16 deny). AC1 is not reachable without teacher spend; stopping on AC3.

Paid options priced with semantscript train --estimate (no request sent, OpenRouter list 2026-09-25), none run (each needs the user's approval; over or near the USD 0.50 cap): (a) three gold examples for stale orders in src/refunds.sem.ts (tried in a scratch edit, reverted; function id becomes nf_950180e6...) regenerates the refund dataset only: 401 requests (max 1320), USD 0.81 (max 2.67); (b) --cases 384 regenerates both: 1192 requests (max 3069), USD 2.02 (max 5.71); (c) a constraints teacher with the OpenRouter teacher as [teacher.fallback]: 271 requests (max 457), USD 0.39 (max 0.78), also regenerates triage and is not 'cached datasets'. Docs: examples/express-app/README.md now describes 217d386c as the release on disk, its 0-of-16 deny grid, the full sweep table, the three priced routes and a short 5c755d08 history (stale 'next train regenerates both datasets' paragraph removed); 217d386c bundle re-measured 2026-09-26 with semantscript package --target lambda-zip: 685,635,429 B (653.9 MiB; node_modules 82.7 MiB, artifact 570.9 MiB), over by 403.9 MiB. docs/deploy.md worked case, docs/tutorial-refund-decision.md steps 4/5 and docs/training-pipeline.md updated to match. semantscript test passes on 217d386c (held-out and seed '-'); releases list shows 217d386c9852, 2/2 passed, 0.9241, 0.0719, 0.

Fix round 1 (2026-09-26): re-verified the blocking findings. They are correct: semantscript explain over the 16-input grid on 217d386c answers approve (paid) and review (fraudulent) for both tiers at 100/120/150/200 days, 0 of 16 deny; releases list shows only 217d386c9852. AC1 (and AC2 for a gated release) cannot be met from the cached datasets, since all 27 sweep runs fail the held-out gate. Every paid route (gold examples USD 0.81, --cases 384 USD 2.02, constraints+OpenRouter fallback USD 0.39 max 0.78, which also regenerates triage) exceeds the USD 0.50 cap or the AC1 wording, so this still waits on the user. No teacher spend this round. Doc fixes from the advisory findings: deploy.md worked case now quotes seed 5, accuracy 0.9241, ECE 0.0719, 0/394 corpus, held-out not recorded, and the releases list line; README dataset IDs are now the content sha256 values the train report prints (caa8e895/6833e7fc/59f21134), with the cache keys given alongside; the price table no longer calls stale-order gold examples the check's suggested fix (the next: line named constraint 3/2/4 in 14 of the 27 runs, including the best, and constraint 0 accounts for 6 of the best run's 20); the stale 'held-out set joins once the gate records one' wording is fixed in README, deploy.md and cli-reference.md (the gate records it; derive does not use it yet); teachers.md no longer says the reduced-prompt regeneration was unmeasured, and names 5c755d08; README, deploy.md and the tutorial say the trained release exists only on the development machine (.semantscript is git-ignored); the tutorial's step 4 warns before the paid command that this expression failed the gate in all 27 retrains. Checks: prettier --check clean, lint:node clean, tests cli 66/66, compiler 89/89, express 3/3.

Fix round 2 (2026-09-26): re-verified the blocking findings; they are correct and still user-gated. explain on 217d386c (standard/fraudulent/150) answers review 0.9571 with 'always "deny" when order.ageDays > 90: VIOLATED by the answer'; releases list still has 217d386c as current; all 27 cached-dataset runs fail the held-out gate (best 20/512). AC1, and AC2 for a gated release, need the user's approval for teacher spend over USD 0.50 or a rescope; no teacher spend this round. Fixed what is fixable without spend: re-derived the int8 release on disk with releases derive --int8 --per-channel --max-decision-change-rate 0.0052 (published f8e22caed3f0 beside 217d386c, current.json unchanged; 3 of 586 decisions changed, 0 of 6 attested, int8 ECE 0.0914, encoder 150,750,065 B, 57 s wall), packaged it into a scratch dir via promote/package/promote-back: 239,708,539 B (228.6 MiB), lambda-zip fits with 21.4 MiB, packaged Lambda handler answered 201 urgent; README and deploy.md size rows now quote f8e22cae (7f982976 kept as history). README: intro and quick-start train line say the held-out gate is not yet passed; the 16-input grid now shown with semantscript explain and its VIOLATED line; says the seed comes from train-report-reduced-seed5.json (manifest has no seed field) and the held-out figure does not exist. Tutorial: step 2 excerpt now matches src/refunds.sem.ts (first example, first two constraints, real prompt); 'gold examples' plural; the failure advice no longer says to add the one examples entry the next: line suggests, and it points keyless readers at the reference application. deploy.md: seed source noted. Checks: lint:node clean, prettier clean, cli 66/66, compiler 89/89, express 3/3, semantscript test passed.

Fix round 3 (2026-09-26): re-verified the blocking findings; correct and still user-gated. current.json manifestSha256 217d386c985212c5...; releases list marks 217d386c9852 current; explain enterprise/fraudulent/200 days answers review with 'always "deny" when order.ageDays > 90: VIOLATED by the answer'. All 27 cached-dataset runs fail the held-out gate (best 20/512), so AC1 and AC2 (for a gated release) need the user's approval of teacher spend over USD 0.50 or a rescope. No teacher spend this round. Advisory fixes: tutorial excerpt comment now names the enterprise 60-day-window deny; step 4 next: advice reconciled (the next: entry is the first step, expect entries for each broken constraint); step 5 says the 217d386c output is the development machine's, not the reader's; 'What you have' mentions the held-out sample and the no-release case. README: explain's verification line has no held-out field (only test and releases show do); int8 section no longer says nothing is published, third row time 56 s (f8e22cae), and explains why releases list prints 0.0719 (float32 figures carried) while int8 ECE 0.0914 is in the derive report. deploy.md: seed 5 inferred from the report file name (no seed field). trainer/README.md and derive.py HELD_OUT_PENDING no longer say a held-out set joins once the gate records one. Checks: prettier clean, ruff clean, lint:node clean, cli 66/66, compiler 89/89, trainer pytest 610 passed 2 skipped.

Finalize (2026-09-26): re-verified. AC3 checked: a python tabulation of all 27 train-report-15.2-*.json shows status failed, heldOutConstraints sampleSize 512, broken inputs 20..109 (per-run figures above), and every report has 0 teacher requests / USD 0; the per-seed and per-variant rates are in these notes. AC1 unchecked: current.json manifestSha256 217d386c985212c5..., releases list marks 217d386c9852 current, and semantscript explain over the 16-input grid answers approve (8 paid) / review (8 fraudulent), 0 of 16 deny. AC2 unchecked: no gated release exists, so there is no held-out figure or recorded seed to quote (docs say 'not recorded'; releases show prints '-'). Blocked on the user: approve OpenRouter spend over USD 0.50 (gold examples for stale orders est. USD 0.81, max 2.67; --cases 384 est. USD 2.02, max 5.71; constraints teacher + OpenRouter fallback est. USD 0.39, max 0.78) or rescope AC1/AC2 (accept 217d386c as wiring-only, change the decision-8 1% tolerance or 512 sample, or allow a USD 0 local qwen3:14b regenerated dataset). Checks: lint:node clean, prettier --check clean, cli tests pass. Status stays In Progress.

Resumed with spend approval (2026-09-26). Setup: copied main checkout examples/express-app/.semantscript into the worktree (main untouched), npm install + build in examples/express-app, wrote .semantscript/teacher-constraints.toml (backend constraints seed 1, [teacher.ranges] order.ageDays 0..240, [teacher.fallback] OpenRouter anthropic/claude-sonnet-5 direct). teacher probe: fallback answered in 2.7 s, USD 0.000072. Estimates (train --estimate, --epochs 16 --select-best-epoch --counterfactual-ratio 0.5, no request sent, OpenRouter price list 2026-09-25): --cases 384: refunds 136 req (max 447) USD 0.28 (max 0.92), triage 381 req USD 0.42, total 517 (max 828) USD 0.70 (max 1.34), ~27 min; --cases 768: total 1033 (max 1650) USD 1.38 (max 2.62), ~53 min. Triage has no constraints so it regenerates entirely through the fallback.

Run (c), 2026-09-26: train --teacher .semantscript/teacher-constraints.toml --cases 384 --epochs 16 --select-best-epoch --counterfactual-ratio 0.5 --max-constraint-violation-rate 0.01 --seed 1 --seed-attempts 3 --device cuda --max-cost-usd 12 --report .semantscript/train-report-15.2-c384-seed1.json. Teacher: 478 requests, 738,080 in (653,526 cached) / 66,155 out tokens, USD 0.9642 (refunds 97 requests USD 0.2173, triage 381 requests USD 0.7469); estimate was USD 0.70 (max 1.34). No fallback twin failure at ratio 0.5. Passed on the first attempt (seed 1): published release 0fd67142d16fab91... decideRefund: 384 cases (3 gold) + 394 adversarial, accuracy 0.9872, ECE 0.0087, pair consistency 0.995, 0 corpus violations, held-out 4/512 (0.78%, gate 1%), epoch 10 of 16. triage: 384 cases (3 gold), accuracy 1.0000, ECE 0.0000. AC1 grid: semantscript explain dist/refunds.sem.js --call decideRefund over standard/enterprise x paid/fraudulent x 100/120/150/200 days (priorRefunds 0, total 100): 16 of 16 deny, constraint 'always deny when order.ageDays > 90' satisfied on each.

Release checks (2026-09-26): semantscript test passed (decideRefund 0.9872/0.0087, held-out 4/512, seed 1; triage 1.0000/0.0000, seed 1; examples 3/3 each); releases list marks 0fd67142d16f current, 2/2 passed, 0.9872, 0.0087, 0. Float32 bundle (package --target lambda-zip): 685,637,515 B (653.9 MiB; node_modules 82.7 MiB, artifact 570.9 MiB), over by 403.9 MiB. releases derive --int8 --per-channel passed the strict default gate: 0 of 1162 decisions changed (778 decideRefund, 384 triage), 0 of 6 attested, worst int8 ECE 0.0047 (float32 0.0039), encoder 569.0 -> 143.8 MiB, 1 min 23 s wall; published c7534774c40f beside it (report .semantscript/derive-report-15.2-c7534774.json). Promoted it, packaged with --include deploy/lambda.mjs: 239,712,504 B (228.6 MiB), lambda-zip fits with 21.4 MiB; packaged Lambda handler answered 201 urgent; explain on the int8 release: 16 of 16 deny on the AC1 grid; promoted 0fd67142 back to current. Express smoke on 0fd67142: POST /refunds/o1 approve committed, /refunds/o2 deny not committed, POST /tickets 201 urgent. npm test in examples/express-app: 3/3.

Docs (AC2), 2026-09-26: examples/express-app/README.md now describes 0fd67142 as the release on disk (teacher-constraints.toml contents, recipe, estimate vs actual cost, rows 384+394 (3 gold), accuracy 0.9872, ECE 0.0087, pair 0.995, 0/778 corpus, 4/512 held-out, seed 1, epoch 10 of 16, 7 fallback-labelled refund cases, releases list/test/explain output, Express route smoke); the 27-run sweep kept as 'Retrains from the older cached datasets failed the held-out check' with the priced routes condensed; 217d386c and 5c755d08 moved to Earlier releases; sizes table and int8 section rewritten for 0fd67142 (685,637,515 B) and c7534774 (239,712,504 B, strict gate, 0 of 1162 changed). docs/tutorial-refund-decision.md step 4 recommends the mixed teacher with its TOML (validated with train --estimate) and the passing recipe, step 4 release paragraph and step 5 quote 0fd67142 (run at 100 days now 'deny'); docs/deploy.md worked case is 0fd67142 with a combined int8 table; docs/teachers.md records the first Anthropic fallback run in mixed mode; docs/training-pipeline.md and docs/diagnostics.md mark 217d386c as the earlier release. Checks: prettier --check docs README.md examples/express-app/README.md clean; lint:node clean; cli 66/66; compiler 89/89 (docs-examples); relative link/anchor scan of touched docs 0 bad.

Commit d208ae3 pushed on task-15.2; CI run 36261677194 green (all 7 jobs). Spend this task: USD 0.9642 (one training run) + USD 0.000072 (probe), total about USD 0.96 of the approved budget; no gold examples or --cases 768 needed. Not in git (git-ignored), to copy back into the main checkout: examples/express-app/.semantscript/artifact (current 0fd67142, int8 c7534774 beside it), .semantscript/cache (new datasets f44bb408/2abb3b15/e85dcfca, teacher-stats, prices), .semantscript/teacher-constraints.toml, .semantscript/train-report-15.2-c384-seed1.json, .semantscript/derive-report-15.2-c7534774.json, .semantscript/derive-report-f8e22cae.json and .semantscript/artifact.derive-report.json. AC1 note: the release comes from a newly generated (paid, approved) dataset, not the cached datasets named in AC1's wording.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Not done: blocked on the user. The Express example was swept over 27 cached-dataset retrains (10 seeds plus epochs, learning-rate, head and batch variants) at USD 0, and none passes the held-out constraint gate (best 20 of 512 against a 1% tolerance). The example still serves 217d386c, which answers deny on 0 of the 16 AC1 grid inputs. The README, tutorial and deploy.md describe that release as it is: seed 5 inferred, accuracy 0.9241, ECE 0.0719, 0/394 corpus violations, held-out not recorded. They also give the sweep table, bundle and int8 sizes (f8e22cae) and the priced paid routes. AC3 is verified; AC1 and AC2 need teacher spend over USD 0.50 or a rescope.
<!-- SECTION:FINAL_SUMMARY:END -->
