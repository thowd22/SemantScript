# CLI

The CLI will expose the `semantscript build`, `train`, `test`, `run`, `dev`, and
`init` workflows. It coordinates compiler, trainer, exporter, and runtime packages
without owning their core implementations.

