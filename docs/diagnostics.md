# Diagnostics catalogue

This page lists every error SemantScript raises with its cause and its fix:
compiler diagnostics at a `sema` site, editor warnings from the
language-service plugin, trainer and verifier failures during
`semantscript train` (including its environment preflight), `test`, `run`
and `build` failures, release management errors, and runtime errors thrown
by compiled calls.

Where a tool prints the fix itself (the `next:` line under a failed
verification gate, the `; next:` clause of every failure `train` and its
trainer print, the `fix:` line under a failed `doctor` check, the `next:`
clause of an editor warning or of a runtime, `test`, `run` or `build`
error), the text comes from one source, `diagnostics/remedies.json`, and so
does the **Printed fix** column of the tables that list those failures:
`node scripts/generate-remedies.mjs` writes the compiler's, the runtime's and
the trainer's remedy tables and those columns, and the test suites fail when
any of them is stale ([contributing](CONTRIBUTING.md#generated-remedies)). A
`<name>` in a printed fix is filled from the failure's evidence. Each failure
that prints a fix is listed once, in a generated table.

Some messages print no fix: the compiler diagnostics, the seed retry's log
lines (the failure they describe carries the `next:` line), release
management refusals and the runtime errors in the class table
(`SemaInputError`, `SemaConfidenceError` and the like). Their **Fix**,
**What to do** and **Advice** columns are written by hand on this page and
say nothing a tool prints. A trained expression that answers wrongly raises no error at all; the
[wrong-answer workflow](#wrong-answer-workflow) at the end takes one from
`semantscript explain` to the example or constraint to add.

## Compiler diagnostics (`TS9100` to `TS9131`)

Every diagnostic names the file, line and column of the `sema` site, the code,
the message and the site text, in TypeScript's own format, so `tsc`, the
build-tool adapters and the editor plugin all show the same thing:

```text
src/refunds.sem.ts(34,86): error TS9122: src/refunds.sem.ts:30:10: constraint predicates support only literals, input/property/index access, comparisons, boolean operators, and finite-number arithmetic (site: sema<RefundDecision>({ … })`…`)
```

| Code | Family                    | Cause                                                                                                                                                                                                                                                                                                                               | Fix                                                                                                                                                                              |
| ---- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 9100 | malformed site            | A `sema` or `sema.withConfidence` tag that is not a canonical site: no output type argument, two type arguments, a configured call with zero or several options arguments, or use through optional chaining (`sema?.`).                                                                                                             | Write `sema<T>\`…\``or`sema<T>({ examples, constraints })\`…\`` with exactly one type argument and, when configured, exactly one inline options object.                          |
| 9101 | unsupported output        | `T` is not a finite scalar: `string`, `number`, `any`, a single literal, an array, a nullable, a union with an unsupported member.                                                                                                                                                                                                  | Use `boolean`, a union of two or more string literals, a homogeneous enum, `Ordinal<[…]>`, `BoundedInt<Min, Max>`, `BoundedNumber<Min, Max, Step>` or a flat interface of those. |
| 9102 | invalid enum              | The enum mixes string and numeric members, or has computed or duplicate values.                                                                                                                                                                                                                                                     | Make every member a constant of one primitive base with distinct values.                                                                                                         |
| 9103 | invalid ordinal           | `Ordinal` without exactly one tuple argument, a tuple of fewer than two members, or members that are not distinct string literals.                                                                                                                                                                                                  | `Ordinal<["low", "medium", "high"]>`: a fixed tuple of at least two distinct string literals.                                                                                    |
| 9104 | invalid bounds            | `BoundedInt`/`BoundedNumber` with minimum not below maximum, a non-integral `(Max - Min) / Step`, a non-finite member or more than 100,000 values.                                                                                                                                                                                  | Choose literal bounds and a step that divide the range exactly into a finite grid of at most 100,000 values.                                                                     |
| 9105 | invalid flat output       | An interface property is optional, a method, an index signature, a nested object, an array, a tuple, `null` or `undefined`, or the interface has no properties.                                                                                                                                                                     | Keep the interface one level deep with required scalar properties of the kinds above, or split it into separate expressions.                                                     |
| 9110 | invalid interpolation     | An interpolation that is not a bare identifier (`${item.value}`, `${f(x)}`, `${a + b}`).                                                                                                                                                                                                                                            | Assign the expression to a named `const` first and interpolate the name; the name becomes the input's name in the IR.                                                            |
| 9111 | duplicate interpolation   | The same identifier is interpolated twice in one template.                                                                                                                                                                                                                                                                          | Interpolate each input once; repeat it in the prose by name instead.                                                                                                             |
| 9112 | unsupported input         | An input whose type cannot be serialized: a function, a class instance with methods, a database client, a recursive type, `unknown`, a symbol, or an object with an index signature.                                                                                                                                                | Pass plain data (primitives, literals, enums, arrays, tuples, objects, unions of those). Read rows first and hand the expression the values.                                     |
| 9120 | invalid options           | The options argument is not an inline object literal, or has a key other than `examples` and `constraints`.                                                                                                                                                                                                                         | `sema<T>({ examples: […], constraints: […] })` with both arrays inline; move shared data into the arrays, not into variables.                                                    |
| 9121 | invalid example           | An example's `inputs` does not cover every interpolated input exactly once, or its `output` is not assignable to `T`.                                                                                                                                                                                                               | Give each example every input by name with a value of the input's type and an output from the support.                                                                           |
| 9122 | invalid constraint        | A constraint is not a direct `always(() => …, output)` or `never(…)` call; its predicate uses something outside literals, input/property/index access, comparisons, boolean operators and finite arithmetic (a function call, a template, a regex); its output is not in the support; or the output is a complete flat interface.   | Rewrite the predicate over the inputs only; inline helpers such as tier windows as boolean arithmetic; for an interface, constrain per-field expressions instead.                |
| 9123 | invalid `@confidence`     | The header is repeated, or is not `@confidence(q)` with a decimal `q` in `[0, 1]`.                                                                                                                                                                                                                                                  | One `@confidence(0.8)` line among the header lines before the behavior text.                                                                                                     |
| 9124 | empty behavior            | The template has no text once headers, interpolations and whitespace are removed.                                                                                                                                                                                                                                                   | Write the behavior: what the expression decides and from what.                                                                                                                   |
| 9125 | contradictory constraints | An `always` and a `never` with the same predicate name the same output.                                                                                                                                                                                                                                                             | Remove one; the pair can never be satisfied.                                                                                                                                     |
| 9126 | invalid `@domain`         | The header is repeated, or is not `@domain(name)` with a lowercase name of letters, digits and dashes.                                                                                                                                                                                                                              | One `@domain(fraud)` line, or drop it and let the file or controller name the domain.                                                                                            |
| 9130 | compilation configuration | The build itself, not a site: `TypeScript skipped JavaScript emission` or `TypeScript emitted no JavaScript output` (the tsconfig has `noEmit`, `emitDeclarationOnly`, or excludes the `.sem.ts` files), a `--domain-depth` whose name is not lowercase letters, digits and dashes, or whose depth is not a positive integer.       | Let the project emit JavaScript for the `.sem.ts` files and fix the option; the message names it.                                                                                |
| 9131 | execution plan            | Provenance analysis failed: `SEMA_EXECUTION_PLAN_CYCLE` (an initializer cycle, or a sema expression that feeds itself through locals), `SEMA_EXECUTION_PLAN_BUDGET` (a data-flow chain deeper than 100 levels or more than 100,000 units of work), or `SEMA_EXECUTION_PLAN_INVALID` (a routed plan whose domains are inconsistent). | Break the cycle or the chain (route the value through a function call, which the analysis does not follow), or simplify the routing.                                             |

One fixture per family is rendered in the compiler's snapshot test
(`compiler/test/fixtures/diagnostics.snapshot.txt`), which is the canonical
wording.

## Editor warnings (`9150` to `9153`)

The language-service plugin adds warnings at a site from the latest artifact.
Each warning ends with `next:` and the fix below:

<!-- remedies:editor:begin -->

| Remedy                   | Failure                                                                | Cause                                                                           | Printed fix                                                                                                                                                                                                                                                                                           |
| ------------------------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `editor-no-artifact`     | `9150`: `sema expression is unverified: no trained artifact at <root>` | No trained artifact under `.semantscript/artifact` (or the `artifact` option).  | `run semantscript train (or semantscript dev) to publish an artifact at <root>; tests can use loadSemaStubArtifact() from @semantscript/core/testing without one`                                                                                                                                     |
| `editor-unreadable`      | `9150`: `the artifact at <root> cannot be read`                        | The pointer or manifest is malformed.                                           | `run semantscript releases list to find an intact release, then switch to it with semantscript releases rollback <release>, which also rewrites a broken current.json (pass --artifact <root> to both when the artifact is not .semantscript/artifact); or publish a new one with semantscript train` |
| `editor-not-in-artifact` | `9151`: `not in the latest artifact`                                   | The expression changed since training, so its id is not in the artifact.        | `run semantscript train; the build cache retrains only the expressions that changed`                                                                                                                                                                                                                  |
| `editor-no-examples`     | `9150`, `9151` or `9152` on an expression without examples             | The expression has no gold examples, which verification and training both need. | `add a few examples: [{ inputs, output }] entries to this sema call, the first thing to try`                                                                                                                                                                                                          |
| `editor-accuracy`        | `9152`: `verified accuracy <a> is below <t>`                           | Verified accuracy below `accuracyThreshold` (default 0.95).                     | `add examples for the cases it misses, or a constraint for the rule it breaks, then retrain (the train report's next: lines name them)`                                                                                                                                                               |
| `editor-ece`             | `9153`: `expected calibration error <e> is above <t>`                  | Verified ECE above `eceThreshold` (default 0.1).                                | `train with more cases (--cases), or set a confidence threshold with a fallback so low-confidence answers are caught`                                                                                                                                                                                 |

<!-- remedies:editor:end -->

## Trainer and verifier failures

Before the bundle driver starts, `train` runs the environment preflight: the
`semantscript doctor` checks for the interpreter, the trainer packages,
PyTorch and its device, ONNX Runtime, the platform environment and the
teacher, without a billed request. A failed check stops the command with exit
1 and `semantscript train: stopped before training`, and the failing line
carries its fix (`--no-preflight` skips it). The
[environment guide](environment.md) lists every check with its causes and
fixes. Every fix a check prints, and the preflight with it:

<!-- remedies:doctor:begin -->

| Remedy                          | Failure                                                                               | Cause                                                                                                                                | Printed fix                                                                                                                                                                                                                                                      |
| ------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `doctor-node`                   | `node`: the running Node is older than the workspace's floor                          | The CLI runs on an older Node release.                                                                                               | `install Node <minimum> or later (the repository's .nvmrc pins the tested release; nvm install)`                                                                                                                                                                 |
| `doctor-runtime-unresolved`     | `runtime-bindings`: `@semantscript/core does not resolve`                             | The runtime package is not installed next to the CLI.                                                                                | `npm install in the project (the CLI loads the runtime from @semantscript/core)`                                                                                                                                                                                 |
| `doctor-runtime-native`         | `runtime-bindings`: `<package> does not load on <platform>`                           | The native ONNX Runtime or tokenizers binary is missing or built for another platform.                                               | `run npm install (or npm rebuild <packages>) on this machine; the native binaries are per platform, so a node_modules copied from another OS or architecture does not load`                                                                                      |
| `doctor-python`                 | `python`: the interpreter does not run, or is older than 3.12                         | `python3` (`python` on Windows, or `--python`, `SEMANTSCRIPT_PYTHON`) is not on the path or is older than 3.12.                      | `install Python 3.12 or later, or point the CLI at one with --python <exe> or SEMANTSCRIPT_PYTHON`                                                                                                                                                               |
| `doctor-trainer-import`         | `trainer`: `<python> cannot import semantscript_trainer`                              | The CLI runs outside the checkout without the package installed.                                                                     | `install the trainer into <python>: pip install "semantscript-trainer[training]" (in a SemantScript checkout: pip install -e ".[training]", or run the CLI from the checkout, which adds trainer/src and model/src to PYTHONPATH)`                               |
| `doctor-trainer-outdated`       | `trainer`: `<module> has no doctor command`                                           | The installed trainer predates this CLI.                                                                                             | `update the SemantScript trainer to the release that matches this CLI, or pass --no-preflight to train without the checks`                                                                                                                                       |
| `doctor-trainer-error`          | `trainer`: `<python> -m <module> doctor exited with status <n>`                       | The trainer's doctor itself failed.                                                                                                  | `run <python> -m <module> doctor to see the full error`                                                                                                                                                                                                          |
| `doctor-trainer-clients`        | `trainer`: `missing anthropic, openai`                                                | The teacher client packages are not installed.                                                                                       | `pip install semantscript-trainer (installs the teacher clients anthropic and openai; in a SemantScript checkout: pip install -e .)`                                                                                                                             |
| `doctor-model`                  | `model`: `semantscript_model does not import`                                         | The model package is not on the interpreter's path.                                                                                  | `pip install semantscript-trainer (in a SemantScript checkout: pip install -e ., or run the CLI from the checkout so model/src is on PYTHONPATH)`                                                                                                                |
| `doctor-training-extra`         | `torch` or `onnxruntime`: a package of the training extra is missing                  | The training extra is not installed into this interpreter.                                                                           | `install the training extra into this interpreter: pip install "semantscript-trainer[training]" (in a SemantScript checkout: pip install -e ".[training]"; for a GPU, install the CUDA or ROCm torch build from https://pytorch.org/get-started/locally/ first)` |
| `doctor-reinstall`              | `torch` or `onnxruntime`: a package is installed but fails to import                  | A broken or partial install, often packages in the user site (`numpy.long`); with `platform-env` failing too, the variable fixes it. | `reinstall <name> into this interpreter (pip install --force-reinstall <name>)`                                                                                                                                                                                  |
| `doctor-platform-env-import`    | `torch`, `device` or `onnxruntime` while `platform-env` fails                         | The import or the GPU works once the variable `platform-env` names is set.                                                           | `<command> (see platform-env)`                                                                                                                                                                                                                                   |
| `doctor-platform-env`           | `platform-env`: `<NAME>=1 needed: <why>`                                              | An environment variable the imports or the GPU need is not set; doctor proved it by retrying with it.                                | `<command>; <persist>`                                                                                                                                                                                                                                           |
| `doctor-device-invalid`         | `device`: `--device <name> is not a trainer device`                                   | An unknown `--device`.                                                                                                               | `pass --device auto, cpu or cuda (ROCm GPUs are cuda to PyTorch)`                                                                                                                                                                                                |
| `doctor-device-query`           | `device`: `querying it failed`                                                        | PyTorch sees a GPU but cannot query it.                                                                                              | `check the GPU driver (nvidia-smi or rocminfo), or pass --device cpu`                                                                                                                                                                                            |
| `doctor-device-missing`         | `device`: `--device cuda requested but torch sees no CUDA or ROCm device`             | A CPU-only torch build, or no GPU driver.                                                                                            | `install the torch build for this GPU (https://pytorch.org/get-started/locally/), or drop --device cuda to train on the CPU`                                                                                                                                     |
| `doctor-device-mps`             | `device` (warning): Apple MPS is available but unused                                 | The trainer does not use MPS yet.                                                                                                    | `expect slower runs; lower --cases or --epochs for a first try`                                                                                                                                                                                                  |
| `doctor-device-cpu`             | `device` (warning): no CUDA or ROCm device                                            | Training runs on the CPU.                                                                                                            | `if this machine has an NVIDIA or AMD GPU, install the matching torch build (https://pytorch.org/get-started/locally/); otherwise lower --cases or --epochs`                                                                                                     |
| `doctor-teacher-file-missing`   | `teacher-config`: `<file> does not exist`                                             | `--teacher` names a file that does not exist.                                                                                        | `write a [teacher] TOML there (docs/teachers.md), pass --teacher <file>, or pass --teacher constraints (lower case) to label with the expressions' own constraints`                                                                                              |
| `doctor-teacher-file-invalid`   | `teacher-config`: `<file> is not a valid teacher file`                                | An invalid `[teacher]` table.                                                                                                        | `fix the [teacher] table (docs/teachers.md lists every key)`                                                                                                                                                                                                     |
| `doctor-teacher-none`           | `teacher-config`: no teacher file and `ANTHROPIC_API_KEY` is not set                  | No teacher is configured.                                                                                                            | `write a [teacher] TOML (docs/teachers.md; Ollama needs no key), set ANTHROPIC_API_KEY to use the default Anthropic teacher, or pass --teacher constraints when the constraints decide every input`                                                              |
| `doctor-teacher-key-openrouter` | `teacher-key`: `ANTHROPIC_API_KEY is not set; OPENROUTER_API_KEY is`                  | The anthropic backend reads only `ANTHROPIC_API_KEY`, and `base_url` points at OpenRouter.                                           | `<command> for this shell (base_url points at OpenRouter, which accepts its own key on the Anthropic route)`                                                                                                                                                     |
| `doctor-teacher-key`            | `teacher-key`: `ANTHROPIC_API_KEY is not set`                                         | An Anthropic teacher or `[teacher.fallback]` without a key (Ollama and the constraints teacher need none).                           | `<command> (an OpenRouter key when base_url is OpenRouter), or switch to the ollama backend (docs/teachers.md)`                                                                                                                                                  |
| `doctor-teacher-pull`           | `teacher-probe`: the Ollama server has no model `<model>`, or the backend answers 404 | The model is not pulled into Ollama.                                                                                                 | `ollama pull <model>`                                                                                                                                                                                                                                            |
| `doctor-teacher-refused`        | `teacher-probe`: the probe got 401 or 403                                             | The key was refused.                                                                                                                 | `the key was refused: check ANTHROPIC_API_KEY (an OpenRouter key when base_url is OpenRouter)`                                                                                                                                                                   |
| `doctor-teacher-model-name`     | `teacher-probe`: the Anthropic model is not found                                     | The model name in the teacher file does not exist.                                                                                   | `check the model name <model> in the teacher file`                                                                                                                                                                                                               |
| `doctor-teacher-ollama-down`    | `teacher-probe`: the Ollama server does not answer                                    | The Ollama server is down, or `base_url` is wrong.                                                                                   | `start the server (ollama serve) or fix base_url in the teacher file`                                                                                                                                                                                            |
| `doctor-teacher-network`        | `teacher-probe`: a connection error or a timeout                                      | The network or `base_url` is wrong.                                                                                                  | `check the network and base_url in the teacher file`                                                                                                                                                                                                             |
| `doctor-teacher-other`          | `teacher-probe`: any other probe failure                                              | The teacher refused the request for another reason.                                                                                  | `see docs/teachers.md`                                                                                                                                                                                                                                           |
| `doctor-venv`                   | An interpreter-level check fails and a `.venv` interpreter exists                     | The packages are in the project's `.venv`, which the default interpreter is not.                                                     | `if the packages are in <venv> rather than <python> (the default interpreter), pass --python <venv> or set the SEMANTSCRIPT_PYTHON environment variable to <venv>`                                                                                               |

<!-- remedies:doctor:end -->

After the preflight, `semantscript train` exits 1 and prints the reason as
one `error:` line that ends with `; next: <fix>`. A bundle that is missing or
not the compiler's, a broken teacher file and the other failures of the
trainer process are listed under
[trainer and interpreter failures](#trainer-and-interpreter-failures). The
failures of an expression's examples, of its constraints under the built-in
constraints teacher (`--teacher constraints` or `backend = "constraints"`, see
[teachers](teachers.md)), of the held-out split and of the teacher spend cap
([teachers](teachers.md#cost-estimate-and-spend-cap)) name the expression as
`<source>:<line> (<id>)` and print these fixes:

<!-- remedies:teacher:begin -->

| Remedy                           | Failure                                                                                                                                         | Cause                                                                                                                                                                                                       | Printed fix                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `train-no-gold-examples`         | `<source>:<line> (<id>) has no gold examples: verification needs at least one attested example per expression, whatever the teacher`            | Verification checks the artifact against the gold examples, so every expression needs one, checked before any generation.                                                                                   | `add an examples: [{ inputs, output }] entry to the sema call, then run semantscript build and semantscript train`                                                                                                                                                                                                                                           |
| `train-gold-example-invalid`     | `<source>:<line> (<id>): IR gold example <i> is invalid: case output violates …`                                                                | A gold example contradicts the expression's own constraints or declared types.                                                                                                                              | `correct the example or the constraint it breaks in the sema call, then run semantscript build and semantscript train`                                                                                                                                                                                                                                       |
| `constraint-constant`            | `constraint <i> is constant true (or false) and has no opposite predicate side` (`UnsynthesizableConstraintError`)                              | The constraint's predicate has the same value for every input the teacher can build, so it has no two-sided boundary to verify.                                                                             | `rewrite the predicate of that constraint over the inputs so that it holds for some inputs and not for others, then run semantscript build and semantscript train`                                                                                                                                                                                           |
| `teacher-constraints-undecided`  | `the constraints do not decide every input, so the constraints teacher cannot label it alone. For the input {…} the constraints admit "a", "b"` | The constraints leave that input open: more than one output is admissible.                                                                                                                                  | `add always/never constraints until exactly one output is admissible for every input, or label the inputs they leave open with a language-model teacher: write a teacher TOML with [teacher] backend = "constraints" and a [teacher.fallback] table (backend = "anthropic" or "ollama" and its model), and pass it with --teacher <file> (docs/teachers.md)` |
| `teacher-constraints-contradict` | `the constraints contradict each other on an input … admit no output`                                                                           | Two active constraints require different outputs, or forbid every one, for the input shown.                                                                                                                 | `correct the predicates or outputs of the constraints active on that input so that exactly one output is admissible, then run semantscript build and semantscript train`                                                                                                                                                                                     |
| `teacher-constraints-none`       | `it declares no constraints, so the constraints teacher cannot label it`                                                                        | The constraints teacher labels an expression only from its always/never constraints.                                                                                                                        | `add always/never constraints that decide every input, or let a language-model teacher label it: write a teacher TOML with [teacher] backend = "constraints" and a [teacher.fallback] table (backend = "anthropic" or "ollama" and its model), and pass it with --teacher <file> (docs/teachers.md)`                                                         |
| `teacher-constraints-object`     | `its output is an object, and the constraints label scalar outputs`                                                                             | Constraints on a flat-interface output are not supported, so the constraints teacher cannot label it.                                                                                                       | `let a language-model teacher label it: write a teacher TOML with [teacher] backend = "constraints" and a [teacher.fallback] table (backend = "anthropic" or "ollama" and its model), and pass it with --teacher <file> (docs/teachers.md)`                                                                                                                  |
| `teacher-constraints-sampling`   | `found <k> of <n> distinct inputs the constraints decide in <m> draws`                                                                          | The sampled inputs are too narrow to reach --cases distinct inputs.                                                                                                                                         | `write a teacher TOML with [teacher] backend = "constraints" and a [teacher.ranges] table that widens the sampling ranges, pass it with --teacher <file> (docs/teachers.md), or request fewer --cases`                                                                                                                                                       |
| `teacher-constraints-twins`      | `found <k> of <n> distinct inputs that the constraints decide and that one field edit moves to another output …`                                | Too few sampled inputs have a single-field twin with another label, as for an expression that always answers the same.                                                                                      | `write a teacher TOML with [teacher] backend = "constraints" and a [teacher.ranges] table that widens the sampling ranges, or twin_filter = false when the constraints always give one output, and pass it with --teacher <file> (docs/teachers.md)`                                                                                                         |
| `training-single-group`          | `<source>:<line> (<id>) needs at least two independent row groups, but its <n> training rows (<k> distinct inputs) form one`                    | Identical inputs, and each case with its counterfactual twin, stay on one side of the held-out split; in a small input space they link every row.                                                           | `lower --counterfactual-ratio (0.5, or 0 when the inputs are a few booleans or enum values), request fewer --cases, or, with the constraints teacher, widen the number ranges in [teacher.ranges] (docs/teachers.md)`                                                                                                                                        |
| `teacher-few-distinct`           | `warning: <source>:<line> (<id>): only <k> distinct inputs among <n> cases`                                                                     | Not an error: the teacher repeated inputs to reach --cases because the input space is small, so held-out accuracy covers fewer inputs than the count suggests.                                              | `widen the input space the teacher samples (a [teacher.ranges] table for the constraints teacher, docs/teachers.md), or request fewer --cases`                                                                                                                                                                                                               |
| `teacher-spend-cap`              | `error: spend cap USD <cap> reached: the next request (about USD <x>) would take the run from USD <y> past it after <n> paid request(s)`        | TeacherBudgetExceeded: the run stopped before the request that would pass --max-cost-usd.                                                                                                                   | `rerun with a higher --max-cost-usd, or without it, to resume: every dataset finished before the stop stays cached in <cache>`                                                                                                                                                                                                                               |
| `teacher-spend-cap-journal`      | `error: spend cap USD <cap> reached: …` with the Anthropic backend                                                                              | As teacher-spend-cap; the Anthropic backend also journals every paid response.                                                                                                                              | `rerun with a higher --max-cost-usd, or without it, to resume: every dataset finished before the stop stays cached in <cache>, and the <count> paid teacher response(s) kept in <journal> replay at no cost`                                                                                                                                                 |
| `teacher-price-unknown`          | `no price is known for teacher model '<model>'`, or OpenRouter's price list was unreachable or does not list the model                          | TeacherPriceUnknown: the model is not in the pinned Anthropic table and not on OpenRouter's list. Without --max-cost-usd or --estimate it is a warning, and the run counts requests and tokens but not USD. | `check the teacher model name, or add a [teacher.pricing] table with input_usd_per_million and output_usd_per_million to the teacher file (docs/teachers.md)`                                                                                                                                                                                                |

<!-- remedies:teacher:end -->

Teacher errors (`TeacherConfigurationError` for a bad TOML or an unsupported
backend, `TeacherTransportError` for a network or API failure,
`TeacherResponseError` for a response that fails the case schema,
`TeacherBatchError`/`TeacherBatchTimeout` for a Message Batch that did not
complete) abort generation rather than producing a partial dataset; a
malformed response is retried up to the adversarial `maximum_attempts` for
boundary and counterfactual proposals, and a synthetic case whose label
violates an active constraint is replaced (three rounds at most) before
`TeacherResponseError` names the constraint. With a `[teacher.fallback]`, a
proposal the fallback got wrong names it
(`the constraints could not build this case, and the [teacher.fallback] teacher (<provider>/<model>) proposed …`).
When the run failed because the teacher's answers were rejected, it also
prints `note: the teacher's answers were rejected, so the <n> journaled response(s) this run used were discarded`,
so a rerun asks the teacher again instead of replaying them.

Lines that are not failures and print no fix: `--max-cost-usd <cap> is below the expected USD <x>: the run will stop before it finishes`
in the `--estimate` table (the cap would stop the run partway; raise it, or
accept a partial run and resume later from the journal), and the
`teacher probe` result, whose failed probe prints the doctor's `teacher-key`
or `teacher-probe` fix from the [doctor table](#trainer-and-verifier-failures).
`--max-cost-usd must be a positive number of USD` is a usage error (exit 2).
The Python API's `train_bundle` also refuses a custom `Teacher` without a
`descriptor` (`teacher must expose a TeacherDescriptor`) and, for an
expression with constraints, one that is not an `AdversarialTeacher`; the CLI's
teachers always are.

### Trainer and interpreter failures

`semantscript train` reads the trainer's error output as it runs. When the
trainer process stops on an uncaught Python exception (a package missing from
the interpreter, an interpreter too old to parse the trainer, any other
crash), the CLI prints the lines before the traceback as they arrive, then one
line in place of the traceback, and keeps the traceback in a file next to the
report:

```text
semantscript train: the trainer stopped: ModuleNotFoundError: No module named 'torch'; next: run semantscript doctor and fix its torch check: python3 cannot import torch; full traceback in /srv/app/.semantscript/artifact.report.traceback.txt
```

The printed `doctor` command carries the run's `--python`, `--trainer-module`
and `--teacher` when they were passed, so it checks the same interpreter,
trainer and teacher file.
A SyntaxError blames the interpreter's version only when it is older than
3.12; on 3.12 or later it is a `trainer-crash`. A traceback the trainer goes on
from (a library logging a handled exception, Python's `Exception ignored in`
at shutdown) is printed where it occurred, and a report the run wrote is
rendered as usual; only a traceback that ends the run without a report is
replaced by the line.

A failure the trainer catches itself prints `error: <message>; next: <fix>`:
a training package that does not import names its doctor check
(`training-extra-missing`, the same check as above), a bundle that is missing
or not the build's names `semantscript build`, a broken teacher file names the
`teacher-config` check, an unreachable teacher names the teacher probe, an
option out of range, a lack of memory, an unwritable path, a failed export and
answers the teacher got wrong each name their own fix, and anything else ends
with `train-failed`. Such a run writes no report, and `train` never renders a
report an earlier run left at the path.

When the operating system kills the trainer (the out-of-memory killer's
`SIGKILL`, a native crash's `SIGSEGV`), there is no Python exception to show:
`train` prints `semantscript train: the trainer was killed by <signal>` with
the `trainer-killed` fix and exits with 128 plus the signal number. Under
`--estimate`, which trains nothing, the same line carries the
`estimate-killed` fix instead, without the training-only `--batch-size` and
`--device`.

A module maps to the doctor check that installs it: `semantscript_trainer`
and the teacher clients to `trainer`, `semantscript_model` to `model`,
`torch`, `transformers`, `tokenizers`, `safetensors` and `numpy` to `torch`,
`onnx` and `onnxruntime` to `onnxruntime`. `train --estimate` wraps its
failures the same way.

<!-- remedies:trainer-process:begin -->

| Remedy                    | Failure                                                                                                                                    | Cause                                                                                                                                                                                                                                                          | Printed fix                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `python-missing`          | `unable to run <python>: spawn <python> ENOENT`                                                                                            | The interpreter the CLI starts does not exist.                                                                                                                                                                                                                 | `run <doctor> and fix its python check: install Python 3.12 or later, or point the CLI at one with --python <exe> or SEMANTSCRIPT_PYTHON`                                                                                                                                                                                                                                 |
| `module-missing`          | `semantscript train: the trainer stopped: ModuleNotFoundError: No module named '<module>'`                                                 | The interpreter lacks a package the trainer imports; the check is trainer, model, torch or onnxruntime depending on the module.                                                                                                                                | `run <doctor> and fix its <check> check: <python> cannot import <module>`                                                                                                                                                                                                                                                                                                 |
| `python-too-old`          | `semantscript train: the trainer stopped: SyntaxError: …`                                                                                  | The interpreter is older than the trainer's syntax (Python 3.12); on 3.12 or later a SyntaxError is a trainer-crash.                                                                                                                                           | `run <doctor> and fix its python check: <python> cannot parse the trainer, which needs Python 3.12 or later`                                                                                                                                                                                                                                                              |
| `trainer-crash`           | `semantscript train: the trainer stopped: <Exception>: <message>`                                                                          | Any other uncaught exception in the trainer.                                                                                                                                                                                                                   | `run <doctor> to check the environment; if every check passes, rerun with --no-cache, and report a bug with the traceback if it fails again`                                                                                                                                                                                                                              |
| `training-extra-missing`  | `error: fine-tuning requires PyTorch; install the project training extra` and the other `… training extra` errors the trainer catches      | A training package (PyTorch, Transformers, tokenizers, safetensors, ONNX) does not import in the trainer's interpreter.                                                                                                                                        | `run <doctor> and fix its <check> check: <python> cannot import <module>`                                                                                                                                                                                                                                                                                                 |
| `bundle-unreadable`       | `error: [Errno 2] No such file or directory: '<bundle>'` or a bundle that is not JSON                                                      | No IR bundle at `--bundle`: `build` has not run, or the path points elsewhere.                                                                                                                                                                                 | `run semantscript build to write the IR bundle, or pass --bundle the build's semantscript.ir.v1.json (now <bundle>)`                                                                                                                                                                                                                                                      |
| `bundle-invalid`          | `error: bundle must be a semantscript.ir-bundle version 1 document` and the other bundle shape errors                                      | The file at `--bundle` is not the compiler's IR bundle, or was edited.                                                                                                                                                                                         | `run semantscript build and pass its semantscript.ir.v1.json as --bundle (now <bundle>); never edit the bundle by hand`                                                                                                                                                                                                                                                   |
| `teacher-config-invalid`  | `error: teacher config file must contain only a [teacher] table` and the other teacher-file errors                                         | The teacher TOML is missing, malformed or has an unknown key.                                                                                                                                                                                                  | `run <doctor> and fix its teacher-config check: <teacher> is not a valid teacher file (docs/teachers.md lists every key)`                                                                                                                                                                                                                                                 |
| `teacher-transport`       | `error: <teacher transport error>` after the teacher stopped answering                                                                     | The teacher's server was unreachable, refused the key or timed out.                                                                                                                                                                                            | `run <doctor> --probe request to test the teacher, then rerun semantscript train: the datasets that finished stay cached`                                                                                                                                                                                                                                                 |
| `trainer-killed`          | `semantscript train: the trainer was killed by <signal>`                                                                                   | The operating system stopped the trainer: SIGKILL is usually the out-of-memory killer, SIGSEGV, SIGBUS or SIGABRT a native crash in PyTorch, ONNX Runtime or the GPU driver.                                                                                   | `run <doctor> and fix its torch and device checks, then rerun with a smaller --batch-size or with --device cpu: the datasets that finished stay cached, so the rerun asks the teacher only for the rest`                                                                                                                                                                  |
| `estimate-killed`         | `semantscript train: the trainer was killed by <signal>` under `--estimate`                                                                | The operating system stopped the trainer while it only estimated the run: --estimate trains nothing and sends no teacher request, so the training flags do not apply: a kill is a crash in the interpreter or a native library, or the host was out of memory. | `run <doctor> and fix its checks, then rerun semantscript train --estimate (it trains nothing and sends no teacher request); if it is killed again, report it as a bug with the signal`                                                                                                                                                                                   |
| `train-out-of-memory`     | `error: … out of memory …` (a PyTorch OutOfMemoryError or a MemoryError)                                                                   | The device or the host ran out of memory while training or exporting.                                                                                                                                                                                          | `rerun with a smaller --batch-size or with --device cpu: the datasets that finished stay cached`                                                                                                                                                                                                                                                                          |
| `encoder-download-failed` | `error: could not load the configured tokenizer: We couldn't connect to 'https://huggingface.co' …` and the other failed encoder downloads | The pinned encoder (or the one --encoder-name and --encoder-revision name) is not in the Hugging Face cache and could not be downloaded: no network, a proxy, a hub outage, offline mode, or a model or revision that does not exist.                          | `check that this machine can reach https://huggingface.co (or the HF_ENDPOINT mirror, through HTTPS_PROXY if one is needed), unset HF_HUB_OFFLINE and TRANSFORMERS_OFFLINE, or copy a Hugging Face cache that holds the encoder into HF_HOME; if you passed --encoder-name or --encoder-revision, check that the model and revision exist; then rerun semantscript train` |
| `train-path-unwritable`   | `error: could not create dataset cache directory: …` and the other file-system errors                                                      | A path the trainer writes (the dataset cache, the artifact root, the report) is not a writable directory, or the disk is full.                                                                                                                                 | `check that <path> can be written (a directory, with write permission and free space), or pass --cache-dir or --artifact another path, then rerun semantscript train`                                                                                                                                                                                                     |
| `artifact-export-failed`  | `error: artifact export failed: …`                                                                                                         | The verified model could not be exported: ONNX Runtime failed, or the ONNX output differs from PyTorch beyond the parity tolerance.                                                                                                                            | `run <doctor> and fix its onnxruntime check; if every check passes, rerun with --no-cache, and report a bug with this message if the export fails again`                                                                                                                                                                                                                  |
| `train-option-invalid`    | `error: counterfactual_ratio must be a finite number from 0 through 1` and the other option range errors                                   | A training option is outside the range the trainer accepts.                                                                                                                                                                                                    | `correct the option the message names and rerun semantscript train (docs/cli-reference.md lists each option and its range)`                                                                                                                                                                                                                                               |
| `teacher-rejected`        | `error: <teacher response or generation error>` after the teacher's answers were rejected                                                  | The teacher answered, but a case broke the contract (outside the declared types, a boundary pair that is not two-sided, a twin that breaks a constraint) on every attempt.                                                                                     | `rerun semantscript train: rejected answers are not replayed, so the teacher is asked again; if it fails the same way, use a stronger teacher model or a lower --counterfactual-ratio (0.5), and check that a single-field edit can cross the predicate of the constraint the message names`                                                                              |
| `train-failed`            | `error: <message>` for any other failure the trainer catches                                                                               | A failure no other remedy classifies.                                                                                                                                                                                                                          | `if the message names a setting or a file, correct it and rerun semantscript train; otherwise run <doctor> to check the environment, and report a bug with this message if every check passes`                                                                                                                                                                            |

<!-- remedies:trainer-process:end -->

### Verification failures

Verification (`VerificationGateError`) fails a function with one line per
gate, each followed by the first failing cases with inputs, expected and
predicted values, and a `next:` line with a suggestion derived from those
cases:

```text
nf_2c6b0e41… verification failures:
  2 gold/human example prediction(s) failed:
    - gold example base:0: inputs {"order":{"status":"cancelled"}} expected "deny", predicted "approve"
    - gold example base:3: inputs {"order":{"status":"cancelled"}} expected "deny", predicted "review"
    next: add the constraint always(() => order.status === "cancelled", "deny"): the 2 missed examples all have order.status === "cancelled" and expect "deny", and every teacher-labelled case where it holds agrees
```

The suggestion is chosen from the evidence, in this order:

- **Underfit head.** When the head gets more than 5% (and at least 3) of its
  own teacher-labelled training cases wrong, or predicts one output for cases
  labelled with several, a gold miss or a corpus or held-out constraint
  violation names more
  training instead of an example: `--epochs` doubled (at least 3) and
  `--cases` doubled (at least 64). One more example would only move the error
  elsewhere.
- **Gold misses.** When two or more misses expect the same output and share
  one input field (the same string or boolean, or all numbers at or above, or
  at or below, a bound), every teacher-labelled case where that predicate
  holds expects the same output, and no constraint states it already, the
  line names the `always(...)` constraint to add. Otherwise, when
  teacher-labelled cases are mispredicted the same way as a missed example
  (the same expected and predicted output), it names one of them as an
  `examples` entry to add. Otherwise it asks to check the example against
  the teacher's labels. A failed verification publishes nothing, but the
  run's dataset is already in the training cache, so on a retrain (a release
  is already published) `semantscript explain` on a call with the example's
  inputs prints the gold example beside the nearest teacher-labelled cases
  from that dataset, marked as not the release's. The function is reported
  missing from the loaded artifact because its id changed, and the `missing`
  line says to act on the `next:` line first: training the unchanged
  expression again reuses the cached dataset and misses the same way. A
  `--no-cache` run caches no dataset, and when the id did not change (a new
  teacher or base model) explain shows the release's dataset instead. On a
  first train there is no release, so explain has nothing to load (the
  [wrong-answer workflow](#wrong-answer-workflow)).
- **Constraint violations.** The constraint broken by the most records: with
  two or more, one of them as an `examples` entry with the output the
  constraint demands; with one, denser boundary data (`--cases` doubled, at least 64).
- **Held-out constraint violations.** The
  [held-out check](training-pipeline.md#held-out-constraint-check) scores each
  constraint on sampled inputs the model never trained on. For the constraint
  broken on the most of them: when the active constraints require exactly
  one output for an offending input, that input as an `examples` entry with
  that output; otherwise `--cases` doubled (at least 64). The failure line
  names the constraints, the seed and the first ten offending inputs with
  the model's answer.
- **ECE.** With fewer than 200 calibration rows, or accuracy of 0.95 or
  more, `--cases` doubled (at least 64); otherwise `--epochs` half again as many.
- **Seed retry.** A violation-rate or ECE failure within the retry margin
  that stopped because the retry was off names `--seed-attempts 3`; one that
  used every attempt names the next untried `--seed`.

The report's `functions[].verification.suggestions` (and
`attempts[].functions[].suggestions`) holds the suggestions one per failure,
and each `failures[]` string ends with its `next:` line. The
`error: verification failed for <id>` line ends with the first one.

<!-- remedies:verifier:begin -->

| Remedy                          | Failure                                                                                                                                                                      | Cause                                                                                                                                                                                                            | Printed fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `gold-rule-constraint`          | `N gold/human example prediction(s) failed`, and two or more misses share a one-field rule                                                                                   | The misses expect the same output and share one input field value or bound, every teacher-labelled case with that field agrees, and no constraint states the rule: the model is missing a rule the data implies. | `add the constraint always(() => <predicate>, <output>): the <count> missed examples all have <predicate> and expect <output>, and every teacher-labelled case where it holds agrees`                                                                                                                                                                                                                                                                                                |
| `gold-repeated-miss-example`    | `N gold/human example prediction(s) failed`, and teacher-labelled cases are mispredicted the same way                                                                        | The model has not learned a region the teacher labelled; the misses repeat on generated cases.                                                                                                                   | `add the examples entry <example>: <count> teacher-labelled case(s) that expect <expected> are also predicted <predicted>`                                                                                                                                                                                                                                                                                                                                                           |
| `gold-check-example`            | `N gold/human example prediction(s) failed`, and no teacher-labelled case repeats the miss                                                                                   | The example disagrees with what the model learned from the teacher's labels: the example, the labels or a constraint is wrong.                                                                                   | `check the example <case> against the teacher's labels (it expects <expected>, the model predicts <predicted>); when a release is already published, semantscript explain <module.js> --call <export> with the example's inputs lists it beside the nearest cached teacher-labelled cases (this run's when the expression changed since that release; a --no-cache run caches none). If the example is right, add examples near it or a constraint for its rule; if not, correct it` |
| `violation-example`             | `N adversarial constraint check(s) failed (…)`, and one constraint is broken by two or more records                                                                          | Raw predictions break the same always/never on several records: the region the constraint covers is thin in the training data.                                                                                   | `add the examples entry <example>: constraint <index> (<source>) is broken by <count> records`                                                                                                                                                                                                                                                                                                                                                                                       |
| `underfit-more-training`        | a gold miss or a constraint violation while the head gets more than 5% of its own teacher-labelled training cases wrong (at least 3), or predicts one output for all of them | The head underfits (too few epochs or cases, or a lowered learning rate), so one more example would only move the error elsewhere.                                                                               | `rerun with --epochs <epochs> (now <currentEpochs>) and --cases <cases> (now <currentCases>), and the default --learning-rate if you lowered it: the head gets <missed> of <total> teacher-labelled training cases wrong (<detail>), which more training fixes before any example does`                                                                                                                                                                                              |
| `violation-denser-data`         | `N adversarial constraint check(s) failed (…)`, and no constraint is broken twice                                                                                            | Isolated violations, usually numbers close to a constraint's boundary.                                                                                                                                           | `sample the boundaries more densely: rerun with --cases <cases> (now <current>), or accept isolated misses by recording a tolerance with --max-constraint-violation-rate`                                                                                                                                                                                                                                                                                                            |
| `held-out-violation-example`    | `held-out constraint check failed on N of M sampled inputs (…)`, and the constraints require exactly one output for an offending input                                       | The model keeps a rule on the inputs it trained on but breaks it on inputs sampled away from them: the training data covers the rule's region only near its boundary.                                            | `add the examples entry <example>: constraint <index> (<source>) is broken on <count> of <size> held-out inputs (seed <seed>), and the constraints require that output for this one; or rerun with --cases <cases> (now <current>) so the region is sampled more densely`                                                                                                                                                                                                            |
| `held-out-violation-more-cases` | `held-out constraint check failed on N of M sampled inputs (…)`, and no single output is required for the offending inputs                                                   | The model breaks a rule on inputs sampled away from its training data, and the constraints alone do not name the answer for them.                                                                                | `rerun with --cases <cases> (now <current>): constraint <index> (<source>) is broken on <count> of <size> held-out inputs (seed <seed>), such as <inputs>; more cases cover the rule's region beyond its boundary, or record a tolerance with --max-constraint-violation-rate`                                                                                                                                                                                                       |
| `calibration-more-cases`        | `ECE x exceeds configured threshold t`, with fewer than 200 calibration rows or a head that already fits                                                                     | Calibration error after temperature scaling is above the gate, measured on few held-out rows.                                                                                                                    | `rerun with --cases <cases> (now <current>): ECE <ece> is measured on <rows> calibration rows, and more cases give more of them`                                                                                                                                                                                                                                                                                                                                                     |
| `calibration-more-epochs`       | `ECE x exceeds configured threshold t`, with 200 or more calibration rows and accuracy below 0.95                                                                            | Calibration error after temperature scaling is above the gate, and the head underfits.                                                                                                                           | `rerun with --epochs <epochs> (now <current>): accuracy <accuracy> shows an underfit head, which calibrates poorly`                                                                                                                                                                                                                                                                                                                                                                  |
| `seed-retry-attempts`           | A violation-rate or ECE failure within the retry margin while the seed retry is off (`--seed-attempts 1`)                                                                    | The failure was narrow, the kind another seed usually fixes, but the retry was turned off.                                                                                                                       | `rerun with --seed-attempts <attempts>: the failure was narrow, and the retry trains the next seeds on the cached datasets without a teacher request`                                                                                                                                                                                                                                                                                                                                |
| `seed-retry-next-seed`          | A violation-rate or ECE failure within the retry margin after every seed attempt failed                                                                                      | Every seed tried failed narrowly.                                                                                                                                                                                | `rerun with --seed <seed>: seeds <first> to <last> failed narrowly, and the retry continues from the next untried seed on the cached datasets`                                                                                                                                                                                                                                                                                                                                       |
| `type-no-cache`                 | `N output type check(s) failed`                                                                                                                                              | A decoded prediction was outside the support; only possible with a corrupted head.                                                                                                                               | `rerun with --no-cache to retrain the head from scratch`                                                                                                                                                                                                                                                                                                                                                                                                                             |

<!-- remedies:verifier:end -->

The seed retry ([CLI reference](cli-reference.md#seed-retry)) reads these
gates after every training attempt and logs one of these lines, which print
no fix of their own:

| Line                                                                                                                                               | Meaning                                                                                                                                          | What to do                                                                                                                                                                      |
| -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `verification failed narrowly at seed <n> (<id>: violation rate r (v of M) within m x tolerance t …); retrying with seed <n+1> (attempt k of K) …` | Every failed gate was the violation rate or the ECE, within the margin. The next seed trains on the cached datasets; no teacher request is sent. | Nothing. The report's `attempts[]` lists every attempt; a build that needs several seeds every time is sensitive to its seed, and denser boundary data (above) makes it robust. |
| `not retrying: <id>: N gold/human example prediction(s) failed; a gold miss is not a seed effect`                                                  | A gold or human example was missed; another seed does not fix a contradiction between the example and the teacher's labels.                      | As for the gold failure above.                                                                                                                                                  |
| `not retrying: <id>: N output type check(s) failed`                                                                                                | A decoded prediction was outside the support.                                                                                                    | As for the type failure above.                                                                                                                                                  |
| `not retrying: <id>: violation rate r (v of M) is outside the retry margin m x t = …` or `… ECE x is outside the retry margin …`                   | The miss is wider than the margin.                                                                                                               | Fix the data or the recipe as above; `--seed-retry-margin` (at most 10) widens the margin when the gate is known to be seed-sensitive.                                          |
| `not retrying: <id>: N constraint violation(s) under a zero tolerance; the margin only widens a nonzero --max-constraint-violation-rate`           | The default tolerance is 0, and m x 0 is 0.                                                                                                      | Fix the data, or record a tolerance with `--max-constraint-violation-rate`.                                                                                                     |
| `not retrying: all K seed attempts failed (seeds a to b); the last failure was narrow: …`                                                          | Every attempt failed narrowly.                                                                                                                   | The failure's `next:` line names the next untried `--seed` (`seed-retry-next-seed` above).                                                                                      |
| `not retrying: seed retry is off (--seed-attempts 1), though the failure was narrow: …`                                                            | The retry is off.                                                                                                                                | The failure's `next:` line names `--seed-attempts` (`seed-retry-attempts` above).                                                                                               |
| `not retrying: seed <n> is the largest seed a split accepts`                                                                                       | The next seed would pass 2^63 - 1, the largest held-out split seed.                                                                              | Start from a smaller `--seed`.                                                                                                                                                  |
| `not retrying: <id>: the violation rate's record count is unknown` or `not retrying: <id>: failed a gate the retry does not classify`              | A defensive stop: the result lacks what the margin needs, or failed a gate the retry cannot classify. Neither is expected from `train`.          | Report it as a bug with the train report attached.                                                                                                                              |
| `error: --seed-attempts: seed attempts must be an integer from 1 through 20` or `error: --seed-retry-margin: seed retry margin must be …` (exit 1) | An out-of-range `--seed-attempts` or `--seed-retry-margin`, checked before the bundle is read (`--estimate` included).                           | The line ends with the `train-option-invalid` fix.                                                                                                                              |

The same stop reason ends the `error: verification failed for <id>; not
retrying: …` line and is the report's `retry.stopReason`; the rendered
report prints `seed retry stopped: <reason>`, and after a retry that passed,
a `training attempts:` table (seed, status, accuracy, violations and ECE per
attempt) and `seed retry: published seed <n> after <k> attempts`. The
published release's manifest records the seed that passed as
`functions[].trainingProvenance.seed`, and `semantscript test` shows it in
its `seed` column.

The trainer's other configuration and export errors print
`error: <message>; next: <fix>` with the generated fix from the
[trainer table](#trainer-and-interpreter-failures) above; nothing here adds
advice of its own:

- An out-of-range option (`--batch-size`, `--evaluation-ratio`,
  `--ece-threshold` and the other training and verification settings)
  raises `TrainingConfigurationError` or `VerificationConfigurationError`
  while `train` checks its options, before the bundle is read, and ends with
  the `train-option-invalid` fix; the message names the option and its
  range. The paragraphs below cover the same errors raised during the run.
- `VerificationConfigurationError` during the run means the corpus cannot
  form a closed evidence set. An expression without a gold example is caught
  before training with `train-no-gold-examples` (add an `examples` entry).
  External cases not disjoint from training inputs end with `train-failed`,
  and the message names what to correct (never attest inputs the teacher
  also generated). `temperature fitting requires held-out calibration rows`
  is an internal-contract error (the held-out split always keeps a row), so
  `train-failed` sends it to `semantscript doctor` and a bug report.
- Export (`ArtifactExportError`) refuses weights changed after verification or
  a tokenizer whose bytes differ, and ends with `artifact-export-failed`
  (`train-path-unwritable` when a file-system error is the cause).
  `ArtifactPublicationError` names a problem under the artifact root (a
  symlink, a path outside the release, a release directory that already
  exists with different content); when a file-system error is the cause it
  ends with `train-path-unwritable`, otherwise with `artifact-export-failed`.
- Training (`TrainingConfigurationError`, `TrainingExecutionError`) during
  the run rejects an unsafe recipe (a checkpoint above the trainable-parameter guard, a batch
  that cannot fit) or a device failure: a memory failure ends with
  `train-out-of-memory`, anything else with `train-failed`, and the message
  names the setting.

## CLI test, run, build and release derivation failures

`semantscript test`, `run`, `build` and `releases derive --int8` end each
failure message with a `next:` clause naming the command that fixes it; a
refused int8 derivation prints the figures first and exits 2. A release that
fails the runtime's load checks makes `semantscript test` exit 1 with the
runtime's own `ArtifactLoadError` code and remedy ([Runtime errors](#runtime-errors))
(`artifact at <root> fails the runtime's load checks: <code>: <detail>; next: <remedy>`).

<!-- remedies:cli:begin -->

| Remedy                     | Failure                                                                                                                                                                                                                                                                                         | Cause                                                                                                                                                                                                                                     | Printed fix                                                                                                                                                                                                                                                                                           |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test-no-artifact`         | `semantscript test`: `no artifact at <root> (current.json is missing)`                                                                                                                                                                                                                          | Nothing was published at the artifact root.                                                                                                                                                                                               | `run semantscript train to publish an artifact at <root>, or pass --artifact for one published elsewhere`                                                                                                                                                                                             |
| `test-example-mismatch`    | `semantscript test --bundle`: `<id> example <n>: expected …, got …`                                                                                                                                                                                                                             | The loaded artifact answers a bundle example differently: it was trained before the example changed, or from another bundle.                                                                                                              | `run semantscript build, then semantscript train, and rerun semantscript test; if it still fails, run semantscript explain <module.js> --call <export> with that example's inputs to see the nearest training cases and constraints, and follow the train report's next: lines`                       |
| `test-function-absent`     | `semantscript test --bundle`: `<id>: absent from the artifact`                                                                                                                                                                                                                                  | The bundle has an expression the artifact was not trained on.                                                                                                                                                                             | `run semantscript train on this bundle (semantscript build first if the source changed)`                                                                                                                                                                                                              |
| `test-function-unverified` | `semantscript test`: a function whose verification status is not `passed`                                                                                                                                                                                                                       | The release carries a function that did not pass its verification gates.                                                                                                                                                                  | `retrain with semantscript train, following the next: lines of its report, or switch to a passing release with semantscript releases rollback <release> (semantscript releases list shows which releases pass)`                                                                                       |
| `test-artifact-unreadable` | `semantscript test`: `current.json.release must be a string`, a missing manifest or another unreadable release                                                                                                                                                                                  | The pointer or the release it names is malformed or missing.                                                                                                                                                                              | `run semantscript releases list to find an intact release, then switch to it with semantscript releases rollback <release>, which also rewrites a broken current.json (pass --artifact <root> to both when the artifact is not .semantscript/artifact); or publish a new one with semantscript train` |
| `test-no-build`            | `semantscript test`: `no semantscript.ir.v1.json under <directories>`                                                                                                                                                                                                                           | Test compares the artifact with the build's bundle, and none was found under the tsconfig outDir, ., dist, out or build.                                                                                                                  | `run semantscript build, then rerun semantscript test; or pass --bundle <path> for a bundle elsewhere, or --no-bundle to check the artifact alone`                                                                                                                                                    |
| `test-function-unbundled`  | `semantscript test`: `<id>: in the artifact but not in the bundle`                                                                                                                                                                                                                              | The artifact has an expression the build's bundle no longer has: the program changed since training.                                                                                                                                      | `the program changed since training: run semantscript build, then semantscript train, and rerun semantscript test; or pass --bundle for the bundle this artifact was trained from`                                                                                                                    |
| `run-no-export`            | `semantscript run --call`: `<module> has no function export named <name>`                                                                                                                                                                                                                       | The name passed to `--call` is not a function the module exports.                                                                                                                                                                         | `pass --call one of the module's function exports: <exports>`                                                                                                                                                                                                                                         |
| `build-no-tsconfig`        | `semantscript build`: `unable to read <tsconfig.json>` or TS5058 / TS5083                                                                                                                                                                                                                       | No TypeScript project at the path; semantscript init wires an existing tsconfig.json but does not create one.                                                                                                                             | `create <config> with npx -p typescript tsc --init --rootDir . --outDir dist, then run semantscript init to add the SemantScript plugins, or pass --project <tsconfig.json>`                                                                                                                          |
| `build-tsconfig-invalid`   | `semantscript build`: TS5092, TS18003 or another error in <tsconfig.json> itself                                                                                                                                                                                                                | The tsconfig.json parses badly or selects no input files.                                                                                                                                                                                 | `correct <config> as the errors above say (it must be one JSON object whose include or files select the .sem.ts sources), then rerun semantscript build`                                                                                                                                              |
| `build-no-outdir`          | `semantscript build`: `<tsconfig.json> must set compilerOptions.outDir`                                                                                                                                                                                                                         | The build writes the compiled calls and the IR bundle under `outDir`.                                                                                                                                                                     | `set compilerOptions.outDir in <config> (for example dist)`                                                                                                                                                                                                                                           |
| `train-no-teacher`         | `semantscript train`: `--teacher is required: no semantscript.teacher.toml, teacher.toml or .semantscript/teacher.toml found and ANTHROPIC_API_KEY is not set`                                                                                                                                  | No teacher file and no key for the default Anthropic teacher.                                                                                                                                                                             | `run semantscript init --teacher anthropic, openrouter, ollama or constraints to write a teacher file, set ANTHROPIC_API_KEY to use the default Anthropic teacher, or pass --teacher constraints when the constraints decide every input`                                                             |
| `teacher-probe-none`       | `semantscript teacher probe`: `no teacher to probe`                                                                                                                                                                                                                                             | No teacher file and no --teacher.                                                                                                                                                                                                         | `run semantscript init --teacher anthropic, openrouter, ollama or constraints to write .semantscript/teacher.toml, or pass --teacher <teacher.toml>`                                                                                                                                                  |
| `int8-gate-refused`        | `semantscript releases derive --int8`: `derive refused: nothing was published and current.json is unchanged`, then the checks that failed: `N attested record(s) changed decision (tolerance T)`, `N of M records changed decision (…)` or `quantized ECE … for <function> exceeds threshold …` | The int8 encoder answered differently from the float32 one on the release's own records (or its calibration drifted) beyond the tolerance, which by default admits no change at all; nothing was published and current.json is unchanged. | `keep serving the current release (current.json is unchanged)<alternative>; to publish anyway, record a tolerance that admits these figures with <command> <tolerance>, which the manifest keeps and semantscript releases show prints`                                                               |
| `int8-records-missing`     | `semantscript releases derive --int8`: `the build cache <dir> has no training dataset <digest> for <function>` or an adversarial dataset it cannot find or pick                                                                                                                                 | The derivation verifies the int8 graph on the records the release was trained on, found in the build cache by the digests the manifest and the cache record; a release trained elsewhere, or whose cache was cleared, has none.           | `pass --cache-dir the build cache the release was trained with (it needs dataset <dataset> for <function>, not found under <cache>); when that cache is gone, retrain with semantscript train so the new release's records are cached, then derive from it`                                           |
| `int8-no-release`          | `semantscript releases derive --int8`: `POINTER_INVALID: no release to derive from at <root> (current.json and releases/ are missing)`                                                                                                                                                          | Nothing was published at the artifact root: the derivation runs before semantscript train, or from another directory than the application's.                                                                                              | `run semantscript train to publish a release at <root>, then derive from it; or pass --artifact for an artifact root published elsewhere`                                                                                                                                                             |
| `int8-release-not-named`   | `semantscript releases derive --int8`: `POINTER_INVALID: <root>/current.json does not exist` while releases are on disk, or `RELEASE_NOT_FOUND: no release under releases/ matches <name>`                                                                                                      | No release is current and none was named, or the name matches no release under releases/.                                                                                                                                                 | `<list> lists the releases on disk; name one of them, as in <command>`                                                                                                                                                                                                                                |

<!-- remedies:cli:end -->

## Release management errors

`semantscript releases` refuses a switch or prune with exit status 1 and a
message that starts with one of `POINTER_INVALID`, `RELEASE_NOT_FOUND`,
`RELEASE_AMBIGUOUS`, `RELEASE_ALREADY_CURRENT`, `RELEASE_NO_PREVIOUS`,
`RELEASE_INTEGRITY`, `RELEASE_UNVERIFIED`, `RELEASE_REJECTED` or
`RELEASE_INVALID`. The [CLI reference](cli-reference.md#releases) gives each
cause; nothing is changed when one is reported.

## Packaging errors

`semantscript package` fails with exit status 1 and a message that starts
with one of `PACKAGE_NO_PROJECT`, `PACKAGE_NO_DIST`, `PACKAGE_NO_RELEASE`,
`PACKAGE_OUT_EXISTS`, `PACKAGE_INCLUDE_MISSING`, `PACKAGE_INSTALL_FAILED`,
`PACKAGE_NO_BINDING` or `PACKAGE_BINDING_LOAD_FAILED`, or with the release
codes above when the current release fails the checks `releases promote`
applies; the output directory is left as it was. `PACKAGE_OVER_TARGET` also
exits 1 but writes the bundle and lists the size levers that would fit. The
[CLI reference](cli-reference.md#package) gives each cause and
[Deploying](deploy.md#the-size-levers) the levers.

## Runtime errors

Compiled calls throw typed errors with a `code`. All are synchronous from the
program's perspective except artifact loading. The errors in this table print
no fix; the advice is written here by hand. The errors for a missing, stale
or unloadable artifact, which do print one, follow in the generated table.

| Class                                          | `code`                                                                                                                                         | Cause                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Advice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SemaArtifactInactiveError`                    | `SEMA_ARTIFACT_INACTIVE`                                                                                                                       | A handle used after `close()` or after a reload replaced it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Hold the handle from `loadSemaArtifact` or use `onReload`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `SemaInputError`                               | `SEMA_INPUT_INVALID`                                                                                                                           | An input does not match the function's declared type (a missing field, a wrong primitive, `NaN`, a cycle) or exceeds the byte limit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Pass exactly the declared inputs; validate request bodies with `require` before the expression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `SemaInferenceError` and subclasses            | `initialization`, `invalid-input`, `timeout`, `closed`, `protocol`, `worker-failed`, `backend`, `invalid-result`, `busy`, `response-too-large` | The inference worker could not start (missing native `onnxruntime-node` for the platform), timed out, was closed, or returned malformed output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Install the runtime's native dependencies on the target platform (`npm ci` there); raise the timeout through the `inference` options of `loadSemaArtifact` for large encoders; check memory.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `SemaConfidenceError`                          | `SEMA_CONFIDENCE_BELOW_THRESHOLD`                                                                                                              | A `@confidence(q)` expression answered below `q` and no fallback is configured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Catch it and decide, use `sema.withConfidence` to see the distribution, or register a fallback in the `fallbacks` map passed to `loadSemaArtifact`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `SemaFallbackError`                            | `SEMA_FALLBACK_INVALID`                                                                                                                        | The fallback returned a value outside the declared type, or re-entered the same expression (reason `cycle`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Return a support value from the fallback and never call the expression it guards.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `SemaStubError` (`@semantscript/core/testing`) | `SEMA_STUB_INVALID`                                                                                                                            | A test stub artifact was described or used inconsistently with the IR bundle; `reason` is `unknown-function`, `unresolved-function` (a compiled function key that calls no or several sema expressions), `invalid-value`, `invalid-confidence` (not above one over the number of possible values, or above 1), `invalid-bundle` (the bundle path is missing or unreadable, is not JSON or not an IR bundle, or `applicationId` is invalid), `unanswered` (a call to a function the stub has no answer for), `unmatched-input` (no `byInput` case and no `otherwise`), `unregistered` (a stub release loaded outside the process that created it) or `occupied-directory` (the given `directory` holds something a stub did not write, such as a trained artifact, including a release published into a directory a crashed stub left behind). Messages name the function by its source position and `nf_` id. | Answer every expression the code under test calls with a support value of its declared type, keyed by the compiled function or the `nf_` id in the message (the source position says which expression it is); key by function id when a function calls several expressions (the message lists each one's source position and id, and the IR bundle has them under `functions[].id` and `functions[].source`); an object of answers is keyed by `nf_` ids only, so key by compiled function with a `Map` or `[function, answer]` pairs; for an id the bundle lacks, rebuild; `compute` must be synchronous; for `invalid-bundle` run `semantscript build` or pass the bundle's real path; for `occupied-directory` omit `directory` or give an empty one; load stubs only through `@semantscript/core/testing`. |
| `RequirementError` (framework)                 | `SEMA_REQUIREMENT_FAILED`                                                                                                                      | A `require(condition, message, status)` guard failed; answered as its status with `{ error, code }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Expected behavior: the guard did its job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `StrictJsonError`                              |                                                                                                                                                | A JSON document the runtime or CLI parses strictly (`--input`, the artifact's pointer and manifest) has duplicate keys or non-finite numbers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Fix the JSON; never edit a release by hand.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

The errors for a missing, stale or unloadable artifact end their message with
`; next: <fix>`, and carry the fix as `error.remedy`; an `ArtifactLoadError`
also carries the message without it as `error.detail`. A failed reload leaves
the previous artifact active, and `semantscript releases rollback <release>`
repairs a bad pointer.

<!-- remedies:runtime:begin -->

| Remedy                  | Failure                                                                                                   | Cause                                                                                                                          | Printed fix                                                                                                                                                                                                                                                                                           |
| ----------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `runtime-not-loaded`    | `SemaRuntimeNotLoadedError` (`SEMA_RUNTIME_NOT_LOADED`)                                                   | A `sema` call before `loadSemaArtifact()` resolved.                                                                            | `await loadSemaArtifact() once at startup, before the first sema call (semantscript run does this for a script); if no artifact exists yet, run semantscript train, or in a test load a stub with loadSemaStubArtifact() from @semantscript/core/testing`                                             |
| `artifact-missing`      | `ArtifactLoadError` (`SEMA_ARTIFACT_PATH`): nothing at the path, or a directory without `current.json`    | No artifact was published at the path: `train` has not run, or the path points elsewhere.                                      | `run semantscript train to publish an artifact at <path>, or point loadSemaArtifact(), --artifact or SEMANTSCRIPT_ARTIFACT at the root train published; a test that needs no training can load a stub with loadSemaStubArtifact() from @semantscript/core/testing`                                    |
| `artifact-path`         | `ArtifactLoadError` (`SEMA_ARTIFACT_PATH`): a symlink, an unreadable entry or a path outside the release  | The artifact root or a release inside it was changed by hand.                                                                  | `point at the artifact root semantscript train published (the directory that holds current.json); never edit or symlink a release by hand`                                                                                                                                                            |
| `artifact-corrupt`      | `ArtifactLoadError` (`SEMA_ARTIFACT_INVALID_JSON`, `_INVALID_POINTER`, `_INVALID_MANIFEST`, `_INTEGRITY`) | The pointer or manifest is malformed, or a file fails its digest.                                                              | `run semantscript releases list to find an intact release, then switch to it with semantscript releases rollback <release>, which also rewrites a broken current.json (pass --artifact <root> to both when the artifact is not .semantscript/artifact); or publish a new one with semantscript train` |
| `artifact-incompatible` | `ArtifactLoadError` (`SEMA_ARTIFACT_INCOMPATIBLE`)                                                        | The artifact was built for another runtime ABI, capability set or ONNX opset.                                                  | `retrain with semantscript train so the artifact matches this runtime (run semantscript build first after a compiler upgrade)`                                                                                                                                                                        |
| `artifact-quota`        | `ArtifactLoadError` (`SEMA_ARTIFACT_QUOTA`)                                                               | A pointer, manifest or resource is larger than the load limits.                                                                | `raise the matching maximum*Bytes option of loadSemaArtifact() if the artifact is expected to be this large`                                                                                                                                                                                          |
| `artifact-resource`     | `ArtifactLoadError` (`SEMA_ARTIFACT_RESOURCE`)                                                            | A resource's tensors, opset or chain are wrong, or the native backend refused it.                                              | `run semantscript doctor --runtime to check the native bindings, then retrain with semantscript train if they pass`                                                                                                                                                                                   |
| `unknown-function`      | `SemaUnknownFunctionError` (`unknown-function`)                                                           | The compiled call's id is not in the loaded artifact: the program was rebuilt after training, or the wrong artifact is loaded. | `the program changed after the artifact was trained: run semantscript build, then semantscript train, and reload; or load the artifact trained from this bundle (semantscript explain lists the artifact functions the bundle no longer has)`                                                         |

<!-- remedies:runtime:end -->

## Wrong-answer workflow

A wrong answer from a trained expression is not an error: the call returns a
valid value from the output type, and the release passed its gates. Start
from the input that answered wrongly and let `semantscript explain` show why:

```sh
semantscript explain dist/refunds.sem.js --call decideRefund \
  --input '[{"tier":"standard","priorRefunds":0},{"ageDays":120,"status":"fraudulent","total":80}]'
```

Against the Express example's earlier release `217d386c`, this printed
(abridged; its successor `0fd67142` answers `"deny"` at 0.9315):

```text
call 1: nf_957c2b2b… (src/refunds.sem.ts:21)
  value        "review"
  confidence   0.9532, uncertainty 0.1957
  distribution "review" 0.9532 | "deny" 0.0355 | "approve" 0.0114
  constraints  2 active of 6 (4 inactive)
    always "deny" when order.ageDays > 90: VIOLATED by the answer
    never "approve" when order.status === "fraudulent": satisfied
  gold examples nearest the input (3 of 3)
    0.209  "review"  {"customer":{…},"order":{"ageDays":3,"status":"fraudulent","total":40}}
    0.437  "approve"  {"customer":{…},"order":{"ageDays":12,"status":"paid","total":88.5}}
    0.876  "deny"  {"customer":{…},"order":{"ageDays":70,"status":"paid","total":900}}
  training cases nearest the input (5 of 394; the release's dataset caa8e8954f69… plus its adversarial sidecar, 2 other cached datasets for this id)
    0.025  "deny"  adversarial constraint-boundary  {… "ageDays":95,"status":"fraudulent" …}
    0.029  "deny"  synthetic (anthropic/claude-sonnet-5)  {… "ageDays":132,"status":"fraudulent" …}
    0.029  "deny"  adversarial counterfactual  {… "ageDays":132,"status":"fraudulent" …}
    0.029  "review"  adversarial constraint-boundary  {… "ageDays":90,"status":"fraudulent" …}
    0.033  "deny"  synthetic (anthropic/claude-sonnet-5)  {… "ageDays":91,"status":"fraudulent" …}
  verification passed: accuracy 0.9241, ECE 0.0719, … 0 constraint violations
```

Read it top to bottom:

1. **Is this the release you think it is?** The `release` line names the
   release directory, build time and manifest digest, and each call its
   dataset, teacher and base model. A `missing` line means the compiled id is
   not in the loaded artifact: the expression changed since training (or the
   wrong artifact is loaded), so the answer you saw came from nowhere you can
   fix by editing examples. Run `semantscript build` and `semantscript train`,
   then explain again. The exception is a train that just failed
   verification: its dataset is cached, so the call's training cases come
   from that run, and the `missing` line says so. Training the same
   expression again reuses that dataset and misses the same way, so read the
   gold examples and training cases shown and apply the example or constraint
   the train's `next:` line names (steps 2 to 4), then train.
2. **Did an active constraint lose?** A `VIOLATED` constraint is a rule you
   already wrote that the model has not learned for this input. The training
   cases near the input show whether it had evidence: in the output above
   every nearest case past 90 days is labelled `deny` (the one `review` is the
   boundary case at exactly 90 days, where the constraint is inactive), so the
   data was right and the model generalised badly past the gold example at
   distance 0.209 that says `review` for a fraudulent order. Add this input as a gold example with the
   output the constraint requires, so verification must reproduce it; train
   with more cases or epochs; and read the verifier's constraint-violation
   rate for the function in the train report.
3. **No constraint covers the input?** When the answer is wrong under a rule
   that decides the input on its own (an amount, a status, a date), write the
   rule down: `always(() => …, output)` or `never(() => …, output)` in the
   `sema` options. The trainer rejects every teacher label that contradicts an
   active constraint and adds boundary cases on both sides of it, so the rule
   reaches the data, not just the prompt. The `could not be evaluated` lines
   name predicates that fail for this input (a missing field, a non-number in
   arithmetic); fix those first.
4. **Are the nearest labels wrong?** Training cases near the input with the
   label you did not expect mean the teacher reads the prompt differently from
   you there: `synthetic (<teacher>)` names who labelled them, and an
   `adversarial counterfactual` pair shows which single field edit flipped the
   label. Add a gold example at this input (and one on the other side of the
   boundary you mean), tighten the prompt text, or add the constraint from step 3. A large
   distance to every gold example says the input is outside what you
   attested; a gold example here is the cheapest fix.
5. **Is the answer just uncertain?** A low confidence with a spread
   distribution is the model saying it does not know. Beyond more examples,
   `@confidence(q)` with a fallback lets the application decide such inputs
   itself; explain reports whether the answer met the threshold.
6. **Retrain and check.** `semantscript train` (the cache reuses every
   unchanged dataset), then run the same `explain` again, and
   `semantscript test` to replay every gold example, including the
   one you just added, through the new release.

"Nearest" in explain is a typed distance over the JSON inputs (numbers scaled
by each field's spread, word overlap for strings, exact match otherwise), not
the model's own similarity, so it finds the labelled cases that look most like
the input rather than the ones the model thinks are alike.
