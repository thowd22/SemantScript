export default function Page() {
  return (
    <main>
      <h1>Ticket triage</h1>
      <p>
        POST a JSON body with <code>subject</code> and <code>body</code> to{" "}
        <code>/api/triage</code>.
      </p>
    </main>
  );
}
